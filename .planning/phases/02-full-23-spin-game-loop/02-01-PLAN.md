---
phase: 02-full-23-spin-game-loop
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/game/scoreTier.ts
  - src/lib/game/scoreTier.test.ts
  - src/lib/game/spinQueue.ts
  - src/lib/game/spinQueue.test.ts
  - src/lib/game/tierColor.ts
  - src/lib/content/types.ts
  - src/lib/session/types.ts
autonomous: true
requirements:
  - CORE-02
  - CORE-03
  - CORE-04
  - REDEM-01

must_haves:
  truths:
    - "scoreTier(1) returns 'F-' and scoreTier(100) returns 'God' with no undefined gaps across integers 1–100"
    - "All 28 grade strings (F- through God) are reachable from some integer in 1–100"
    - "computeOverallScore() returns a value in [1, 100] for any mix of stat scores"
    - "buildInitialQueue() returns exactly 22 entries with 'race' first and 'title' last, no ability slots"
    - "Queue splice at currentSpinIndex + 1 inserts N entries at the correct position without disrupting entries after the splice point"
    - "FlavorLabel, Race, Archetype, Weakness, and SimpleItem types are all exported from src/lib/content/types.ts"
    - "SpinResult and SessionState in types.ts accept optional tier, score, spinQueue, currentSpinIndex fields without breaking existing field access"
  artifacts:
    - path: "src/lib/game/scoreTier.ts"
      provides: "scoreTier, gradeToScore, computeOverallScore, TierGrade, TIER_THRESHOLDS, STAT_WEIGHTS"
      exports: ["scoreTier", "gradeToScore", "computeOverallScore", "TierGrade"]
    - path: "src/lib/game/scoreTier.test.ts"
      provides: "Unit tests covering all 28 grade boundaries and computeOverallScore"
      contains: "scoreTier(1) === 'F-'"
    - path: "src/lib/game/spinQueue.ts"
      provides: "SpinCategory, SpinDefinition, buildInitialQueue, getSegmentsForCategory"
      exports: ["SpinCategory", "SpinDefinition", "buildInitialQueue", "getSegmentsForCategory"]
    - path: "src/lib/game/spinQueue.test.ts"
      provides: "Unit tests covering queue length, order, and splice behavior"
      contains: "buildInitialQueue"
    - path: "src/lib/game/tierColor.ts"
      provides: "tierToCssVar mapping all 28 TierGrade values to CSS variable strings"
      exports: ["tierToCssVar"]
    - path: "src/lib/content/types.ts"
      provides: "FlavorLabel, Race, Archetype, SimpleItem, Weakness interfaces"
      exports: ["FlavorLabel", "Race", "Archetype", "SimpleItem", "Weakness"]
    - path: "src/lib/session/types.ts"
      provides: "Extended SpinResult with tier?/score? and SessionState with spinQueue?/currentSpinIndex?"
      contains: "spinQueue?"
  key_links:
    - from: "src/lib/content/types.ts"
      to: "src/lib/game/scoreTier.ts"
      via: "import type { TierGrade } from '$lib/game/scoreTier'"
      pattern: "TierGrade"
    - from: "src/lib/session/types.ts"
      to: "src/lib/game/scoreTier.ts"
      via: "import type { TierGrade } from '$lib/game/scoreTier'"
      pattern: "TierGrade"
    - from: "src/lib/session/types.ts"
      to: "src/lib/game/spinQueue.ts"
      via: "import type { SpinDefinition } from '$lib/game/spinQueue'"
      pattern: "SpinDefinition"
---

<objective>
Establish every contract type and pure utility that all subsequent Phase 2 plans depend on:
the 28-grade tier mapping, spin queue data model, tier color lookup, and content interfaces.
No UI, no content data — only the mathematical and type foundations.

Purpose: Plans 02-02 through 02-05 all import from these files. They cannot execute without
well-defined types and passing unit tests proving the invariants that CLAUDE.md mandates
(scoreTier as single source of truth, all 28 grades reachable).

Output: 7 files. 5 new utility/type modules. 2 extended existing files. All pure functions
covered by unit tests that pass with `npx vitest run src/lib/game/`.
</objective>

<execution_context>
@/Users/bastianpethel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bastianpethel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game/.planning/PROJECT.md
@/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game/.planning/ROADMAP.md
@/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game/.planning/phases/01-animation-foundation/SKELETON.md
@/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game/CLAUDE.md

<interfaces>
<!-- Phase 1 contracts — do not change these; extend only where specified -->

From src/lib/session/types.ts (current — extend this file):
```typescript
export type SpinStatus = 'IDLE' | 'SPINNING' | 'LANDED' | 'REVEALED'

export interface WeightedSegment {
  label: string
  weight: number
  color?: string
}

export interface SpinResult {
  step: number
  category: string
  resultLabel: string
  resultIndex: number
  timestamp: string
}

export interface SessionState {
  sessionId: string
  startedAt: string
  completedSpins: SpinResult[]
}
```

From src/lib/game/random.ts (file pattern to mirror for new utilities):
```typescript
export interface WeightedItem { label: string; weight: number }
export function weightedRandom(items: WeightedItem[]): number { ... }
```

From src/lib/game/geometry.ts (multi-export utility file pattern to mirror for spinQueue.ts):
```typescript
export function slicePath(...): string { ... }
export function equalSegmentAngles(...): Array<...> { ... }
export function calculateTargetAngle(...): number { ... }
```

From src/lib/game/spinEngine.ts (const-data + export pattern for content modules):
```typescript
import type { SpinStatus } from '../session/types'
const VALID_TRANSITIONS: Record<SpinStatus, SpinStatus[]> = { ... }
export function isValidTransition(...): boolean { ... }
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: scoreTier.ts + scoreTier.test.ts — 28-grade pure mapping</name>
  <files>src/lib/game/scoreTier.ts, src/lib/game/scoreTier.test.ts</files>
  <read_first>
    - src/lib/game/random.ts — file structure pattern (no imports, named exports, fallback return)
    - src/lib/game/random.test.ts — test structure pattern (describe/it/expect, fixture const, loop coverage)
    - CLAUDE.md — "scoreTier() is the single source of truth" rule
    - .planning/phases/02-full-23-spin-game-loop/02-RESEARCH.md — Pattern 3 (TIER_THRESHOLDS), Pattern 9 (STAT_WEIGHTS)
  </read_first>
  <behavior>
    RED — write these tests first, run `npx vitest run src/lib/game/scoreTier.test.ts` (must fail — file missing):
    - scoreTier(1) === 'F-'
    - scoreTier(100) === 'God'
    - Loop integers 1–100: every call returns a string that is one of the 28 known TierGrade values (no undefined, no error)
    - No integer 1–100 is unmapped — the loop above produces exactly 100 valid results with zero undefined
    - scoreTier(3) === 'F-', scoreTier(4) === 'F', scoreTier(99) === 'SSS+', scoreTier(100) === 'God'
    - gradeToScore('F-') returns a number between 1 and 3 inclusive
    - gradeToScore('God') returns 100
    - gradeToScore applied to every TierGrade returns a number in [1, 100]
    - computeOverallScore({ strength: 75, speed: 75, agility: 75, durability: 75, iq: 75, charisma: 75, fightingSkill: 75, potential: 75, energyLevel: 75, powerMastery: 75, weaponMastery: 75 }) returns 75
    - computeOverallScore({}) returns 1 (all stats missing, weighted sum = 0, clamped to 1)
    - computeOverallScore with any stat scores in [1,100] returns a value in [1, 100]

    GREEN — implement scoreTier.ts to pass all tests:
    Export TierGrade union type (28 strings: 'F-','F','F+','E-','E','E+','D-','D','D+','C-','C','C+','B-','B','B+','A-','A','A+','S-','S','S+','SS-','SS','SS+','SSS-','SSS','SSS+','God').
    Export TIER_THRESHOLDS array of {min, max, grade} using exact ranges from RESEARCH.md Pattern 3.
    Export STAT_WEIGHTS Record with keys: strength(0.10), speed(0.10), agility(0.10), durability(0.10), iq(0.08), charisma(0.07), fightingSkill(0.12), potential(0.10), energyLevel(0.08), powerMastery(0.08), weaponMastery(0.07) — sum must equal 1.00.
    Export scoreTier(score: number): TierGrade — clamp to [1,100], iterate TIER_THRESHOLDS, return matching grade, fallback 'F-'.
    Export gradeToScore(grade: TierGrade): number — find entry in TIER_THRESHOLDS, return Math.round((min+max)/2), fallback 1.
    Export computeOverallScore(statResults: Record<string, number>): number — weighted sum using STAT_WEIGHTS, clamp result to [1,100] with Math.max(1, Math.min(100, ...)), return Math.round.
    No default export. No imports from other project files.
  </behavior>
  <action>
    Write test file first (scoreTier.test.ts). Imports: `import { describe, it, expect } from 'vitest'` and named imports from './scoreTier'. Include a constant ALL_GRADES array of all 28 grade strings for use in loop assertions. Write three describe blocks: 'scoreTier', 'gradeToScore', 'computeOverallScore'. Run tests to confirm RED. Then implement scoreTier.ts following the random.ts file structure pattern (no imports, named exports only, pure functions, no try/catch, fallback safety valve). TIER_THRESHOLDS ranges from RESEARCH.md Pattern 3: F-(1-3), F(4-6), F+(7-9), E-(10-12), E(13-15), E+(16-18), D-(19-21), D(22-24), D+(25-27), C-(28-31), C(32-35), C+(36-39), B-(40-44), B(45-49), B+(50-54), A-(55-59), A(60-64), A+(65-69), S-(70-74), S(75-78), S+(79-82), SS-(83-86), SS(87-89), SS+(90-92), SSS-(93-95), SSS(96-97), SSS+(98-99), God(100-100).
  </action>
  <verify>
    <automated>cd "/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game" && npx vitest run src/lib/game/scoreTier.test.ts</automated>
  </verify>
  <done>
    npx vitest run src/lib/game/scoreTier.test.ts exits 0 with all tests green.
    scoreTier.ts exports TierGrade union, scoreTier, gradeToScore, computeOverallScore, TIER_THRESHOLDS, STAT_WEIGHTS.
    scoreTier(1) === 'F-', scoreTier(100) === 'God', no integer 1–100 returns undefined.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: spinQueue.ts + spinQueue.test.ts — queue data model and segment resolver</name>
  <files>src/lib/game/spinQueue.ts, src/lib/game/spinQueue.test.ts</files>
  <read_first>
    - src/lib/game/geometry.ts — multi-function export structure to mirror (check actual file contents)
    - src/lib/game/geometry.test.ts — test structure to mirror (describe per function, concrete assertions)
    - src/lib/session/types.ts — WeightedSegment interface that getSegmentsForCategory must return
    - .planning/phases/02-full-23-spin-game-loop/02-RESEARCH.md — Pattern 1 (SpinDefinition, buildInitialQueue), Pattern 5 (getSegmentsForCategory)
  </read_first>
  <behavior>
    RED — write these tests first in spinQueue.test.ts, run (must fail — file missing):
    - buildInitialQueue() returns an array with length 22
    - buildInitialQueue()[0].category === 'race'
    - buildInitialQueue()[0].displayName === 'Race'
    - buildInitialQueue()[21].category === 'title'
    - buildInitialQueue() contains no entry with category 'racialAbility' or 'archetypeAbility'
    - Splice simulation: take initial queue copy, splice 3 racialAbility entries at index 1 with splice(1, 0, slot1, slot2, slot3), assert new length is 25, assert entries at indices 1/2/3 have category 'racialAbility', assert entry at index 4 has category 'archetype'
    - Splice simulation with 1 entry: length becomes 23, entry at index 1 is 'racialAbility', entry at index 2 is 'archetype'
    - Splice simulation with 4 entries: length becomes 26, entries at indices 1-4 are 'racialAbility', entry at index 5 is 'archetype'
    - getSegmentsForCategory('strength') returns an array with length > 0 (after content modules exist; skip/stub this test with a TODO note in Wave 1 since content modules don't exist yet — test only that the function returns an array)
    - getSegmentsForCategory with an unknown category returns [] (empty array, no throw)

    GREEN — implement spinQueue.ts:
    Export SpinCategory union type with all 23 category strings: 'race' | 'racialAbility' | 'archetype' | 'archetypeAbility' | 'backstory' | 'height' | 'strength' | 'speed' | 'agility' | 'durability' | 'iq' | 'charisma' | 'fightingSkill' | 'power' | 'powerMastery' | 'weapon' | 'weaponMastery' | 'weaponEnchantment' | 'potential' | 'energyLevel' | 'weakness' | 'redemptionSpin' | 'title'.
    Export SpinDefinition interface: { category: SpinCategory; displayName: string }.
    Export buildInitialQueue(): SpinDefinition[] returning 22 entries in the order from RESEARCH.md Pattern 1 (Race, Archetype, Backstory, Height, Strength, Speed, Agility, Durability, IQ, Charisma, FightingSkill, Power, PowerMastery, Weapon, WeaponMastery, WeaponEnchantment, Potential, EnergyLevel, Weakness, RedemptionSpin, Title — no ability slots).
    Export getSegmentsForCategory(category: SpinCategory): WeightedSegment[] — import WeightedSegment from '$lib/session/types'; import content arrays lazily where available; switch on category and return the matching array cast to WeightedSegment[]; return [] for unknown categories. In Wave 1 the content files do not exist yet, so stub all non-test categories by returning [] and add a comment marking each stub for replacement in Plan 02-02/02-03.
  </behavior>
  <action>
    Write test file first (spinQueue.test.ts). Imports: `import { describe, it, expect } from 'vitest'` and named imports from './spinQueue'. The splice simulation tests should work on plain JavaScript arrays (no Svelte reactivity needed in tests — just Array.from(buildInitialQueue()), then call .splice() directly). Run tests (RED). Implement spinQueue.ts: no default export, named exports only, mirror geometry.ts structure. For getSegmentsForCategory, stub every category to return [] initially — each stub is marked with a comment like `// TODO: replace with import from '$lib/content/strength-labels'` so Plan 02-03 can do targeted replacements. Import WeightedSegment type from '$lib/session/types'. Do not import from content modules that don't exist yet.
  </action>
  <verify>
    <automated>cd "/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game" && npx vitest run src/lib/game/spinQueue.test.ts</automated>
  </verify>
  <done>
    npx vitest run src/lib/game/spinQueue.test.ts exits 0 with all tests green.
    spinQueue.ts exports SpinCategory, SpinDefinition, buildInitialQueue, getSegmentsForCategory.
    buildInitialQueue() returns length-22 array with 'race' at [0] and 'title' at [21], no ability slots.
    Splice tests confirm insertions land at the correct index without disrupting subsequent entries.
  </done>
</task>

<task type="auto">
  <name>Task 3: tierColor.ts, content/types.ts, and session/types.ts extension</name>
  <files>
    src/lib/game/tierColor.ts,
    src/lib/content/types.ts,
    src/lib/session/types.ts
  </files>
  <read_first>
    - src/lib/session/types.ts — current content; must extend, not replace
    - src/lib/game/scoreTier.ts — TierGrade union must be imported here (just created in Task 1)
    - .planning/phases/02-full-23-spin-game-loop/02-UI-SPEC.md — tier CSS variable names (lines 96–124), tierToCssVar full 28-entry map (lines 138–170)
    - .planning/phases/02-full-23-spin-game-loop/02-RESEARCH.md — Pattern 6 (content types), Pattern 10 (session extension), Pattern 2 (FlavorLabel)
    - .planning/phases/02-full-23-spin-game-loop/02-PATTERNS.md — "src/lib/content/types.ts" section (imports only TierGrade, pure interface exports), "src/lib/session/types.ts" section (optional field extension)
  </read_first>
  <action>
    Create src/lib/game/tierColor.ts: import type { TierGrade } from './scoreTier'. Export function tierToCssVar(grade: TierGrade): string with a const map: Record<TierGrade, string> containing all 28 grade-to-CSS-variable mappings verbatim from UI-SPEC.md lines 141–169. Fallback: return map[grade] ?? 'var(--tier-f-minus)'. No default export.

    Create src/lib/content/types.ts: import type { TierGrade } from '$lib/game/scoreTier'. Export interfaces: Race { label: string; weight: number; abilitySpinCount: number; weaknessProbabilityModifier: number; description?: string }, Archetype { label: string; weight: number; abilitySpinCount: number; description?: string }, SimpleItem { label: string; weight: number; description?: string }, Weakness extends SimpleItem { severe: boolean }, FlavorLabel { label: string; weight: number; color?: string; tier: TierGrade; score: number }. No functions, no constants, no default export — types only.

    Extend src/lib/session/types.ts: add two optional fields to SpinResult: tier?: TierGrade and score?: number. Add two optional fields to SessionState: spinQueue?: SpinDefinition[] and currentSpinIndex?: number. Add necessary imports at the top: import type { TierGrade } from '$lib/game/scoreTier' and import type { SpinDefinition } from '$lib/game/spinQueue'. Use 'import type' to avoid circular runtime dependency risk. Do not modify WeightedSegment or SpinStatus.
  </action>
  <verify>
    <automated>cd "/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game" && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/game/tierColor.ts exports function tierToCssVar(grade: TierGrade): string
    - tierToCssVar contains exactly 28 entries in the const map (one per TierGrade value)
    - tierToCssVar('F-') returns 'var(--tier-f-minus)', tierToCssVar('God') returns 'var(--tier-god)', tierToCssVar('SSS+') returns 'var(--tier-sss-plus)'
    - src/lib/content/types.ts exports FlavorLabel, Race, Archetype, SimpleItem, Weakness interfaces
    - FlavorLabel has fields: label (string), weight (number), color? (string), tier (TierGrade), score (number)
    - Race has fields: label, weight, abilitySpinCount, weaknessProbabilityModifier, description?
    - Weakness extends SimpleItem and adds severe: boolean
    - src/lib/session/types.ts SpinResult now has optional tier?: TierGrade and score?: number fields
    - src/lib/session/types.ts SessionState now has optional spinQueue?: SpinDefinition[] and currentSpinIndex?: number fields
    - npx svelte-check exits with 0 errors (no TypeScript errors across the project)
    - Existing store.test.ts still passes: `npx vitest run src/lib/session/store.test.ts` exits 0
  </acceptance_criteria>
  <done>
    tierColor.ts created with 28-entry map. content/types.ts created with 5 interfaces. session/types.ts extended with 4 optional fields. svelte-check exits 0. store.test.ts still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 2 is entirely browser-only. No network requests, no user authentication, no server-side execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | localStorage (wof_session key) | accept | Session data is game state only — no PII, no auth tokens, no financial data. A user tampering with their own localStorage only affects their own game session. No server validates it in Phase 2. |
| T-02-02 | Information Disclosure | scoreTier.ts + content data | accept | All game logic and content is bundled client-side and intentionally public. No secrets exist in this phase. |
</threat_model>

<verification>
Full utility layer check — run after all three tasks complete:

```bash
cd "/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game" && npx vitest run src/lib/ && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5
```

Expected: all vitest tests green, svelte-check 0 errors.
</verification>

<success_criteria>
- npx vitest run src/lib/game/scoreTier.test.ts exits 0 — all 28 grade boundaries covered
- npx vitest run src/lib/game/spinQueue.test.ts exits 0 — queue length and splice position tests pass
- npx vitest run src/lib/session/store.test.ts exits 0 — existing tests unbroken by type extension
- npx svelte-check exits with 0 TypeScript errors across the project
- scoreTier.ts: 5 named exports (TierGrade, TIER_THRESHOLDS, scoreTier, gradeToScore, computeOverallScore)
- spinQueue.ts: 4 named exports (SpinCategory, SpinDefinition, buildInitialQueue, getSegmentsForCategory)
- tierColor.ts: 1 named export (tierToCssVar) with 28-entry map
- content/types.ts: 5 interface exports (Race, Archetype, SimpleItem, Weakness, FlavorLabel)
- session/types.ts: SpinResult has tier? and score?; SessionState has spinQueue? and currentSpinIndex?
</success_criteria>

<output>
After completion, create `.planning/phases/02-full-23-spin-game-loop/02-01-SUMMARY.md`
</output>
