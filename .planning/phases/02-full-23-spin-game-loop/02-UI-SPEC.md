---
phase: 2
slug: full-23-spin-game-loop
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-15
---

# Phase 2 — UI Design Contract: Full 23-Spin Game Loop

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Tailwind CSS 4 utility classes only |
| Preset | not applicable |
| Component library | none |
| Icon library | none (text labels only in Phase 2) |
| Font | System sans-serif stack (Tailwind default) |

**Rationale:** Project uses Svelte 5 (not React), so shadcn is not applicable. Tailwind CSS 4 is already installed via `@tailwindcss/vite`. No custom `tailwind.config` exists — all tokens are expressed as Tailwind utility classes inline. Phase 2 introduces custom CSS variables in `app.css` for the 28-grade tier color palette only; everything else remains Tailwind utilities.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Tailwind Class | Usage |
|-------|-------|----------------|-------|
| xs | 4px | `gap-1` / `p-1` | Icon gaps, tight badge padding |
| sm | 8px | `gap-2` / `p-2` | Badge internal padding, compact rows |
| md | 16px | `gap-4` / `p-4` | Default card/panel element spacing |
| lg | 24px | `gap-6` / `p-6` | Card padding, section separators |
| xl | 32px | `gap-8` / `p-8` | Layout column gap (wheel + results panel) |
| 2xl | 48px | `gap-12` / `py-12` | Page vertical breathing room |
| 3xl | 64px | `gap-16` / `py-16` | Not used in Phase 2 |

**Exceptions:**
- Touch targets for "Spin Wheel" and "Next Spin" buttons: minimum 44px height (use `py-3` = 12px + 20px line-height = 44px, enforced)
- SVG wheel size: 400×400px fixed (from Phase 1 — do not change)
- Announcement banner: 8px top/bottom padding, full width — uses `py-2 px-4`

---

## Typography

| Role | Size | Weight | Line Height | Tailwind Class |
|------|------|--------|-------------|----------------|
| Display (overall tier grade) | 72px | 700 | 1.0 | `text-7xl font-bold leading-none` |
| Heading (page title, card title, category names, card section headers) | 28px | 700 | 1.2 | `text-3xl font-bold leading-tight` |
| Body (result labels, descriptions, stat rows, results panel subheadings) | 16px | 400 | 1.5 | `text-base font-normal leading-relaxed` |
| Label/Meta (numeric score, timestamps, counts, secondary info) | 14px | 400 | 1.4 | `text-sm font-normal` |

**Rules:**
- Exactly 2 font weights: 400 (regular) for body text, label/meta, and descriptions; 700 (bold) for display size, headings, and any element that previously used subheading weight.
- Results panel subheadings that require hierarchy use Body (16px) at weight 700 (`text-base font-bold`) — smaller size but bold weight achieves sufficient visual hierarchy without a third weight.
- All text is `text-white` on dark surfaces. Muted text uses `text-gray-400` only.
- The overall tier grade letter (e.g., "God", "SSS+") renders at Display size in CharacterCard hero — this is the single largest typographic element on the page.

> **Footnote — inherited Phase 1 SVG values (not declared in Phase 2 CSS typography):**
> The SpinWheel SVG segment text uses `font-size="13" font-weight="600"` — these are SVG presentation attributes locked in `src/components/SpinWheel.svelte` from Phase 1. The 13px size and weight 600 are not Phase 2 CSS typography declarations and must not appear in this table or be counted against the 4-size / 2-weight limits. Do not change them.

---

## Color

### Surface Colors (60/30/10 split)

| Role | Hex | Tailwind Class | Usage |
|------|-----|----------------|-------|
| Dominant (60%) | `#030712` (gray-950) | `bg-gray-950` | Page background — entire screen surface |
| Secondary (30%) | `#111827` (gray-900) | `bg-gray-900` | Results panel background, CharacterCard background, modal/resume prompt background |
| Tertiary surface | `#1f2937` (gray-800) | `bg-gray-800` | Individual stat rows in results panel, inner card sections |
| Accent (10%) | `#4f46e5` (indigo-600) | `bg-indigo-600` | Spin Wheel button, Next Spin button, active state only |
| Destructive | `#dc2626` (red-600) | `bg-red-600` | "New Character" button (destructive — resets all state) |

**Accent reserved for:** Spin Wheel button (active/enabled state only), Next Spin button (active/enabled state only). No other element uses indigo-600 as a background. Disabled states use `opacity-40` on the same color, not a different color.

**Destructive reserved for:** "New Character" button only. No inline confirmation — the button resets immediately (per D-06: no page refresh, no modal). The destructive color is the sole warning signal.

### Tier Color Palette (28 grades)

The tier color palette is the most important design decision in Phase 2. Colors key off rarity tier grouping: F/E/D = dull earth tones, C/B = neutral blues, A = green, S = gold, SS = amber/orange, SSS = purple, God = radiant white-gold.

Define these as CSS custom properties in `src/app.css` inside a `@layer base` block:

```css
@layer base {
  :root {
    --tier-f-minus: #6b4226;
    --tier-f:       #7c3d1a;
    --tier-f-plus:  #92400e;
    --tier-e-minus: #78350f;
    --tier-e:       #854d0e;
    --tier-e-plus:  #713f12;
    --tier-d-minus: #4b5563;
    --tier-d:       #374151;
    --tier-d-plus:  #4b5563;
    --tier-c-minus: #1e40af;
    --tier-c:       #1d4ed8;
    --tier-c-plus:  #2563eb;
    --tier-b-minus: #065f46;
    --tier-b:       #047857;
    --tier-b-plus:  #059669;
    --tier-a-minus: #15803d;
    --tier-a:       #16a34a;
    --tier-a-plus:  #22c55e;
    --tier-s-minus: #a16207;
    --tier-s:       #ca8a04;
    --tier-s-plus:  #eab308;
    --tier-ss-minus: #b45309;
    --tier-ss:       #d97706;
    --tier-ss-plus:  #f59e0b;
    --tier-sss-minus: #7e22ce;
    --tier-sss:       #9333ea;
    --tier-sss-plus:  #a855f7;
    --tier-god:     #fef9c3;
  }
}
```

**Usage of tier colors:**
- `FlavorLabel.color` for each stat wheel segment = `var(--tier-{grade})` (derived from the label's embedded `tier` field)
- `TierBadge` background = `var(--tier-{grade})`
- `TierBadge` text = white for all tiers except God (God uses `#1a1a1a` dark text for contrast on the pale gold background)
- The wheel pointer (triangle) remains `#FF6B35` (locked from Phase 1)
- Selected segment stroke on landing remains `gold` with `stroke-width: 4` (locked from Phase 1)

**Tier grade → CSS variable name mapping function:**

```typescript
// src/lib/game/tierColor.ts (new utility)
export function tierToCssVar(grade: TierGrade): string {
  const map: Record<TierGrade, string> = {
    'F-':   'var(--tier-f-minus)',
    'F':    'var(--tier-f)',
    'F+':   'var(--tier-f-plus)',
    'E-':   'var(--tier-e-minus)',
    'E':    'var(--tier-e)',
    'E+':   'var(--tier-e-plus)',
    'D-':   'var(--tier-d-minus)',
    'D':    'var(--tier-d)',
    'D+':   'var(--tier-d-plus)',
    'C-':   'var(--tier-c-minus)',
    'C':    'var(--tier-c)',
    'C+':   'var(--tier-c-plus)',
    'B-':   'var(--tier-b-minus)',
    'B':    'var(--tier-b)',
    'B+':   'var(--tier-b-plus)',
    'A-':   'var(--tier-a-minus)',
    'A':    'var(--tier-a)',
    'A+':   'var(--tier-a-plus)',
    'S-':   'var(--tier-s-minus)',
    'S':    'var(--tier-s)',
    'S+':   'var(--tier-s-plus)',
    'SS-':  'var(--tier-ss-minus)',
    'SS':   'var(--tier-ss)',
    'SS+':  'var(--tier-ss-plus)',
    'SSS-': 'var(--tier-sss-minus)',
    'SSS':  'var(--tier-sss)',
    'SSS+': 'var(--tier-sss-plus)',
    'God':  'var(--tier-god)',
  }
  return map[grade] ?? 'var(--tier-f-minus)'
}
```

### Semantic State Colors

| State | Color | Usage |
|-------|-------|-------|
| Disabled | `opacity-40` on button color | Spin Wheel/Next Spin when not actionable |
| Border (cards) | `border-gray-600` | Resume prompt, panels |
| Border (active/highlighted) | `border-indigo-500` | Resumed session indicator (from Phase 1 pattern) |
| Muted text | `text-gray-400` | Meta text, category sub-labels |
| Announcement banner | `bg-indigo-900 border border-indigo-500` | Queue expansion notice (D-12) |

---

## Layout Architecture

This section is Claude's discretion (per CONTEXT.md). The layout contract must be followed exactly.

**The 400×400px SpinWheel SVG is the primary visual anchor during active spins.** All surrounding layout elements — buttons, results panel, category header — are secondary to this focal point and must not compete for spatial dominance.

### Game Screen Layout (during spins 1 through N-1)

```
┌─────────────────────────────────────────────────────────┐
│  [Page background: bg-gray-950]                         │
│                                                         │
│  "Wheel of Fate"  [heading]        Spin 4 of 26  [meta] │
│  "Next: Strength"  [body bold]                          │
│                                                         │
│  ┌──────────────────────┐  ┌──────────────────────────┐ │
│  │   SpinWheel SVG      │  │  Results Panel           │ │
│  │   400×400px          │  │  bg-gray-900             │ │
│  │                      │  │  max-h-[400px]           │ │
│  │   [Spin Wheel] btn   │  │  overflow-y-auto         │ │
│  │   [Result label]     │  │                          │ │
│  │   [Next Spin btn]    │  │  [Race]    Human  C+  36 │ │
│  │                      │  │  [R.Abil]  Flight S  75  │ │
│  │                      │  │  [Archetype] Mage A+ 67  │ │
│  └──────────────────────┘  └──────────────────────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**Desktop (>=768px):** Two-column layout. Left column: wheel + buttons. Right column: results panel. `flex flex-row gap-8 items-start`.

**Mobile (<768px):** Single column. Wheel on top, results panel below as a fixed-height scrollable list. `flex flex-col gap-6`.

**Column widths:** Wheel column `max-w-sm` (384px), Results panel `flex-1 min-w-0 max-w-xs`.

### Announcement Banner (D-12)

Displayed between the wheel result and the Next Spin button, when Race or Archetype has just landed and ability spins are being added. Replaces the Next Spin button momentarily — appears on top, button moves below it.

```
┌────────────────────────────────────────────┐
│  bg-indigo-900 border border-indigo-500    │
│  rounded-lg py-2 px-4 text-center          │
│                                            │
│  "Your race grants 3 ability spins!"       │
│  [text-indigo-300 text-sm font-bold]       │
└────────────────────────────────────────────┘
[Next Spin] button appears below
```

The announcement does NOT auto-dismiss. It persists until the user clicks "Next Spin", at which point `showAnnouncement = null` and `currentSpinIndex` advances.

### Category Header

Before each spin, shown above the wheel area. Replaces the subtitle with the upcoming spin's display name:

- **During session:** `"Next: {displayName}"` in `text-base font-bold text-gray-300`
- **Just landed (REVEALED):** `"{displayName} Result:"` in `text-base font-bold text-white`

### Results Panel

- Container: `bg-gray-900 rounded-xl p-4 max-h-[400px] overflow-y-auto`
- Each result row: `flex items-center gap-3 py-2 border-b border-gray-800 last:border-0`
- Row layout: `[CategoryLabel 80px] [TierBadge 48px] [score 32px] [result label flex-1]`
- Newest result appears at the top (prepend, not append) — so the user sees the most recent spin without scrolling
- Non-stat spins (Race, Archetype, Powers, Weapons, Backstory, etc.) show result label but no TierBadge — badge column is empty for these

### Character Card (post-session)

Replaces the full game screen when `currentSpinIndex >= spinQueue.length`.

```
┌──────────────────────────────────────────────────────┐
│  bg-gray-900 rounded-2xl p-8 max-w-2xl mx-auto       │
│                                                       │
│  ┌──────────────────────────────┐                    │
│  │  OVERALL TIER HERO (D-05)    │                    │
│  │  Grade: "SSS+"               │  [72px bold]       │
│  │  Score: 94 / 100             │  [16px muted]      │
│  │  TierBadge bg-color          │                    │
│  └──────────────────────────────┘                    │
│                                                       │
│  ─────────────── Stats ─────────────                  │
│  Strength       [B+] 52   "Iron Sinew"                │
│  Speed          [S]  75   "Phantomstep"               │
│  ...                                                  │
│                                                       │
│  ─────────────── Other ─────────────                  │
│  Race           Human                                 │
│  Archetype      Shadow Dancer                         │
│  Power          Aggressive Sneezing                   │
│  Weakness       Vague Social Anxiety                  │
│  Title          "The Perpetually Confused"            │
│  ...                                                  │
│                                                       │
│  [New Character]  bg-red-600 hover:bg-red-700        │
└──────────────────────────────────────────────────────┘
```

**Hero tier badge layout:** Full-width centered block at top of card. Grade letter(s) at `text-7xl font-bold leading-none`. Score at `text-sm text-gray-400 mt-1`. The badge itself has `rounded-xl p-6` with the tier background color filling it.

---

## Component Inventory

All new components for Phase 2:

### TierBadge.svelte

Props: `grade: TierGrade`, `score?: number`, `hero?: boolean`

- Default (non-hero): `inline-flex items-center justify-center rounded px-2 py-0.5 text-xs font-bold w-12 h-6` — background = `tierToCssVar(grade)`, text white (or dark for God)
- Hero variant (`hero=true`): `rounded-xl p-6 text-7xl font-bold leading-none text-center w-full` — background = `tierToCssVar(grade)`
- God tier text color: `#1a1a1a` (dark, not white — pale gold background)
- All other tiers: `text-white`

### CharacterCard.svelte

Props: `results: SpinResult[]`

- Full character sheet rendered after all spins complete
- Contains hero TierBadge, two sections (stat rows + non-stat rows), "New Character" button
- Stat rows: category name + TierBadge + numeric score + result label
- Non-stat rows: category name + result label (no badge)
- "New Character" button: `bg-red-600 hover:bg-red-700 text-white px-6 py-3 rounded-lg font-bold text-base`

### SpinWheel.svelte (Phase 1 — no changes)

Locked from Phase 1. `segments`, `onSpinComplete` props unchanged. The `FlavorLabel[]` for stat wheels satisfies `WeightedSegment[]` structurally — pass directly. Tier colors are embedded in `FlavorLabel.color`, so the wheel naturally shows tier density through color.

---

## Interaction Contract

### Spin Flow State Machine

```
IDLE → [user clicks Spin Wheel] → SPINNING → LANDED → [500ms] → REVEALED
  ↑                                                                    │
  └──────────────────── [user clicks Next Spin] ──────────────────────┘
                              (clears announcement if any)
```

| State | Spin Wheel button | Next Spin button | Announcement | Results panel |
|-------|-------------------|-----------------|--------------|---------------|
| IDLE | Enabled (indigo-600) | Hidden | Hidden | Visible |
| SPINNING | Disabled (opacity-40) | Hidden | Hidden | Visible |
| LANDED | Disabled (opacity-40) | Hidden | Hidden | Visible |
| REVEALED | Hidden | Enabled (indigo-600) | Shown if race/archetype | Visible (new row added) |

### Transition Between Spins

When "Next Spin" is clicked:

1. `currentSpinIndex` increments
2. Category header text changes — no animation, immediate swap
3. Spin Wheel button appears (enabled) — no animation
4. `SpinWheel` receives new `segments` prop — wheel stays at its current rotation (per monotonic accumulation rule)

There is no cross-fade or GSAP transition between spins. The state swap is instant. The GSAP animation only runs when the user clicks Spin Wheel. This preserves maximum tension — the user decides when to look at the next wheel.

**Single exception:** The announcement banner (D-12) uses a CSS fade-in: `opacity-0 animate-[fadeIn_0.3s_ease-out_forwards]`. Define in `app.css`:

```css
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(-4px); }
  to   { opacity: 1; transform: translateY(0); }
}
```

### Spin Counter Display

Format: `Spin {currentSpinIndex + 1} of {spinQueue.length}` — `text-sm text-gray-400` — top right of the game area. Updates reactively as the queue expands (queue length increases when ability spins are spliced in).

### Resume Prompt (inherited from Phase 1)

Visual pattern unchanged from Phase 1:
- `bg-gray-800 border border-gray-600 rounded-xl p-6` container
- Two buttons: "Resume Session" (`bg-indigo-600`) and "Start Over" (`bg-gray-600`)
- Shows last spin result + count of completed spins

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (active spin) | "Spin Wheel" |
| Primary CTA (advance) | "Next Spin" |
| Category header (pre-spin) | "Next: {displayName}" |
| Category header (post-spin) | "{displayName} — Result:" |
| Spin counter | "Spin {n} of {total}" |
| Announcement — race | "Your race grants {n} ability spin{s}!" |
| Announcement — archetype | "Your archetype grants {n} ability spin{s}!" |
| Character card title | "Your Character" |
| Overall tier label | "Overall Tier" |
| New Character button | "New Character" |
| Resume prompt heading | "You have a saved session" |
| Resume prompt body | "Last spin: {label} ({n} spin{s} completed)" |
| Resume button | "Resume Session" |
| Start Over button | "Start Over" |
| Empty results panel | "Your fate is being written..." |
| Results panel heading | "Your Results" |
| Stats section heading (card) | "Stats" |
| Other results section heading (card) | "Character Details" |

**Tone rules:**
- No exclamation marks except in the queue-expansion announcement (D-12) — the drama lives in the wheel itself
- No "Click to" prefix — buttons are self-describing verbs
- "New Character" not "Reset" or "Restart" — reinforces the fate/character-creation framing
- Character card uses definite framing: "Your Character" not "Character Result" or "Session Complete"

**Destructive action:** "New Character" resets all state without confirmation (per D-06). The red button color is the sole warning signal. No modal, no "Are you sure?". This is intentional — matches the fatalistic game tone.

**Error state:** Not applicable in Phase 2. All game logic is local; there is no network call. localStorage errors fail silently (the existing `store.ts` already handles `typeof localStorage === 'undefined'`). No error state copy needed.

---

## scoreTier() Numeric Ranges (Claude's Discretion)

Locked by RESEARCH.md Pattern 3. Reproduced here for the UI contract:

| Grade | Score Range | Badge Color Group |
|-------|-------------|-------------------|
| F- | 1–3 | Dark brown |
| F | 4–6 | Dark brown |
| F+ | 7–9 | Dark brown |
| E- | 10–12 | Dark amber-brown |
| E | 13–15 | Dark amber-brown |
| E+ | 16–18 | Dark amber-brown |
| D- | 19–21 | Gray-blue |
| D | 22–24 | Gray-blue |
| D+ | 25–27 | Gray-blue |
| C- | 28–31 | Mid blue |
| C | 32–35 | Mid blue |
| C+ | 36–39 | Mid blue |
| B- | 40–44 | Green |
| B | 45–49 | Green |
| B+ | 50–54 | Green |
| A- | 55–59 | Bright green |
| A | 60–64 | Bright green |
| A+ | 65–69 | Bright green |
| S- | 70–74 | Gold-yellow |
| S | 75–78 | Gold-yellow |
| S+ | 79–82 | Gold-yellow |
| SS- | 83–86 | Amber-orange |
| SS | 87–89 | Amber-orange |
| SS+ | 90–92 | Amber-orange |
| SSS- | 93–95 | Purple |
| SSS | 96–97 | Purple |
| SSS+ | 98–99 | Purple |
| God | 100 | Pale gold (dark text) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | none | not applicable — no component registry used |

No shadcn, no third-party component registry. All components are hand-authored Svelte files. Registry safety gate is not applicable for this phase.

---

## Pre-Population Sources

| Decision | Source |
|----------|--------|
| Dark surface color (`bg-gray-950`) | Extracted from `src/routes/+page.svelte` Phase 1 baseline |
| Accent color (`bg-indigo-600`) | Extracted from `src/routes/+page.svelte` and `SpinWheel.svelte` Phase 1 baseline |
| Wheel segment text: 13px, weight 600 | Locked from `src/components/SpinWheel.svelte` (do not change) — inherited Phase 1 SVG value, not a Phase 2 CSS declaration |
| Wheel COLORS palette (8 colors) | Locked from `src/components/SpinWheel.svelte` (replaced in Phase 2 by tier-keyed colors) |
| "Next Spin" button controls pacing | CONTEXT.md D-01 |
| Category name shown before each spin | CONTEXT.md D-02 |
| Growing results list alongside wheel | CONTEXT.md D-03 |
| Character card: tier badge + numeric score + result label per stat | CONTEXT.md D-04 |
| Overall tier badge = hero element (grade + score) | CONTEXT.md D-05 |
| "New Character" button, no page refresh | CONTEXT.md D-06 |
| Queue expansion announcement copy | CONTEXT.md D-12 |
| 28-grade tier system (F- through God) | REQUIREMENTS.md CORE-04 |
| scoreTier() numeric ranges | RESEARCH.md Pattern 3 (Claude's discretion per CONTEXT.md) |
| Stat weights for overall score | RESEARCH.md Pattern 9 (Claude's discretion) |
| Results list layout (sidebar/scrollable) | Claude's discretion per CONTEXT.md |
| Tier color palette | Claude's discretion per CONTEXT.md |
| Spin transition behavior (none/instant) | Claude's discretion per CONTEXT.md |
| Results panel scrollable fixed-height | Claude's discretion per CONTEXT.md |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
