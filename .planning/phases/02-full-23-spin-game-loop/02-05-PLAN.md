---
phase: 02-full-23-spin-game-loop
plan: 05
type: execute
wave: 4
depends_on:
  - 02-04
files_modified:
  - src/components/TierBadge.svelte
  - src/components/CharacterCard.svelte
autonomous: false
requirements:
  - CARD-01
  - REDEM-01

must_haves:
  truths:
    - "Character card shows a hero overall tier badge with letter grade (e.g. 'SSS+') at 72px bold and numeric score (e.g. '94 / 100') beneath it"
    - "Each of the 11 stat results shows a TierBadge (grade), numeric score, and result label in a row"
    - "Non-stat results (Race, Archetype, Power, Weapon, etc.) show category name and result label but no badge"
    - "Overall tier and score are computed via computeOverallScore() and scoreTier() — never stored independently"
    - "TierBadge background color matches the tier's CSS custom property (--tier-b-plus for B+, etc.)"
    - "God tier badge uses dark text (#1a1a1a) — all other tiers use white text"
    - "'New Character' button resets all game state without a page reload — spin counter returns to 'Spin 1 of 22'"
    - "TierBadge works in both hero (large, full-width) and inline (small, badge-sized) modes"
  artifacts:
    - path: "src/components/TierBadge.svelte"
      provides: "Reusable grade badge component; hero and inline variants; tier-colored background"
      exports: []
    - path: "src/components/CharacterCard.svelte"
      provides: "Full styled character sheet; hero TierBadge; stat rows; non-stat rows; New Character button"
      exports: []
  key_links:
    - from: "src/components/CharacterCard.svelte"
      to: "src/lib/game/scoreTier.ts"
      via: "import { scoreTier, computeOverallScore } from '$lib/game/scoreTier'"
      pattern: "computeOverallScore"
    - from: "src/components/TierBadge.svelte"
      to: "src/lib/game/tierColor.ts"
      via: "import { tierToCssVar } from '$lib/game/tierColor'"
      pattern: "tierToCssVar"
    - from: "src/routes/+page.svelte"
      to: "src/components/CharacterCard.svelte"
      via: "<CharacterCard {results} onNewCharacter={handleNewCharacter} />"
      pattern: "CharacterCard"
---

<objective>
Replace the stub CharacterCard and TierBadge components with full styled implementations.
Delivers the end-of-session screen: a real designed character sheet with tier-colored badges,
per-stat rows, and the "New Character" reset flow.

Purpose: This is the payoff moment of the entire Phase 2 game loop. The user has spun through
20+ wheels and now sees the full character card with all results, tier badges, and an overall
grade computed from the weighted stat average. Without this plan, the game ends on a placeholder.

Output: TierBadge.svelte (reusable, 2 variants) and CharacterCard.svelte (full character sheet).
Both replace the stubs created in Plan 02-04. Followed by a human verification checkpoint of
the complete session — the Phase 2 success criteria visual check.
</objective>

<execution_context>
@/Users/bastianpethel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bastianpethel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game/.planning/REQUIREMENTS.md
@/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game/CLAUDE.md
@/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game/.planning/phases/02-full-23-spin-game-loop/02-UI-SPEC.md
@/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game/.planning/phases/02-full-23-spin-game-loop/02-CONTEXT.md
@/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game/.planning/phases/02-full-23-spin-game-loop/02-04-SUMMARY.md

<interfaces>
<!-- Contracts that CharacterCard and TierBadge must satisfy -->

From src/lib/session/types.ts (Plan 02-01 extension):
```typescript
export interface SpinResult {
  step: number
  category: string
  resultLabel: string
  resultIndex: number
  timestamp: string
  tier?: TierGrade     // present for stat spins; absent for race, archetype, power, etc.
  score?: number       // present for stat spins; absent for non-stat spins
}
```

From src/lib/game/scoreTier.ts (Plan 02-01):
```typescript
export type TierGrade = 'F-' | 'F' | 'F+' | 'E-' | 'E' | 'E+' | 'D-' | 'D' | 'D+' |
  'C-' | 'C' | 'C+' | 'B-' | 'B' | 'B+' | 'A-' | 'A' | 'A+' | 'S-' | 'S' | 'S+' |
  'SS-' | 'SS' | 'SS+' | 'SSS-' | 'SSS' | 'SSS+' | 'God'
export function scoreTier(score: number): TierGrade
export function computeOverallScore(statResults: Record<string, number>): number
```

From src/lib/game/tierColor.ts (Plan 02-01):
```typescript
export function tierToCssVar(grade: TierGrade): string
// Returns CSS var strings: 'var(--tier-f-minus)', 'var(--tier-god)', etc.
```

TierBadge props contract (to be implemented in this plan — replacing the stub):
```typescript
let { grade, score, hero = false }: {
  grade: TierGrade
  score?: number
  hero?: boolean
} = $props()
```

CharacterCard props contract (to be implemented in this plan — replacing the stub):
```typescript
let { results, onNewCharacter }: {
  results: SpinResult[]
  onNewCharacter: () => void
} = $props()
```

From src/components/SpinWheel.svelte (structural reference for component file layout):
```typescript
// File structure: <script lang="ts"> with $props(), $derived values
// Then template using Tailwind utilities, no <style> block needed
```
</interfaces>

<ui_spec_excerpts>
## TierBadge Visual Spec (from UI-SPEC.md)

Non-hero (inline badge in results panel and character card stat rows):
- `inline-flex items-center justify-center rounded px-2 py-0.5 text-xs font-bold w-12 h-6`
- background-color: tierToCssVar(grade) via inline style
- text color: white for all tiers except God (God: #1a1a1a dark text)

Hero (overall tier badge in character card):
- `rounded-xl p-6 text-center w-full`
- Grade letter: `text-7xl font-bold leading-none` — this is the "Display" typography size
- Score: `text-sm text-gray-400 mt-1` — shows "{score} / 100"
- background-color: tierToCssVar(grade) via inline style

## CharacterCard Visual Spec (from UI-SPEC.md)

Container: `bg-gray-900 rounded-2xl p-8 max-w-2xl mx-auto w-full`
Title: `<h2 class="text-3xl font-bold mb-6">Your Character</h2>`

Overall tier section:
- Label above hero badge: `<p class="text-sm text-gray-400 mb-2">Overall Tier</p>`
- Hero TierBadge (grade + score) — full width

Stats section (11 stat results only):
- Section heading: `<h3 class="text-base font-bold mt-8 mb-3 text-gray-300">Stats</h3>`
- Each row: `flex items-center gap-3 py-2 border-b border-gray-800 last:border-0`
- Row content: `[category name 100px] [TierBadge inline] [score 32px] [result label flex-1]`
- Category name: `text-sm text-gray-400 w-24 shrink-0 capitalize` (format: 'strength', 'fightingSkill' → show as "Fighting Skill")

Character Details section (non-stat results):
- Section heading: `<h3 class="text-base font-bold mt-6 mb-3 text-gray-300">Character Details</h3>`
- Each row: `flex items-start gap-3 py-2 border-b border-gray-800 last:border-0`
- Row content: `[category name 100px] [result label flex-1]` (no badge, no score)

New Character button:
- `<button onclick={onNewCharacter} class="mt-8 w-full bg-red-600 hover:bg-red-700 text-white py-3 rounded-lg font-bold text-base transition-colors">New Character</button>`
</ui_spec_excerpts>

<stat_categories_for_card>
// Categories that produce stat rows (have tier + score in SpinResult):
const STAT_CATEGORIES = ['strength', 'speed', 'agility', 'durability', 'iq',
  'charisma', 'fightingSkill', 'potential', 'energyLevel', 'powerMastery', 'weaponMastery']

// Display name formatting for stat categories:
// 'fightingSkill' → 'Fighting Skill', 'powerMastery' → 'Power Mastery', etc.
// Simple transform: insert space before capital letters, then capitalize first letter
// e.g.: name.replace(/([A-Z])/g, ' $1').trim()

// The STAT_WEIGHTS used in computeOverallScore:
const STAT_WEIGHTS: Record<string, number> = {
  strength: 0.10, speed: 0.10, agility: 0.10, durability: 0.10, iq: 0.08,
  charisma: 0.07, fightingSkill: 0.12, potential: 0.10, energyLevel: 0.08,
  powerMastery: 0.08, weaponMastery: 0.07
}
// computeOverallScore() from scoreTier.ts handles this internally.
// CharacterCard just calls computeOverallScore(statResultsRecord).
</stat_categories_for_card>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TierBadge.svelte — reusable tier grade badge with hero and inline variants</name>
  <files>src/components/TierBadge.svelte</files>
  <read_first>
    - src/components/TierBadge.svelte — current stub content (must read to know what to replace)
    - src/lib/game/tierColor.ts — tierToCssVar function and return format
    - src/lib/game/scoreTier.ts — TierGrade union type
    - .planning/phases/02-full-23-spin-game-loop/02-UI-SPEC.md — TierBadge component spec (non-hero vs hero variants, God text color)
    - .planning/phases/02-full-23-spin-game-loop/02-PATTERNS.md — TierBadge.svelte section (exact Svelte template structure)
    - src/components/SpinWheel.svelte — $props() pattern to mirror (lines 15–18)
  </read_first>
  <action>
    Replace the stub src/components/TierBadge.svelte with the full implementation. No onMount, no GSAP — this is a pure display component.

    Script section: import { tierToCssVar } from '$lib/game/tierColor'; import type { TierGrade } from '$lib/game/scoreTier'. Props (using $props() pattern from SpinWheel.svelte lines 15–18): let { grade, score, hero = false }: { grade: TierGrade; score?: number; hero?: boolean } = $props(). Derived values: let bgColor = $derived(tierToCssVar(grade)); let textColor = $derived(grade === 'God' ? '#1a1a1a' : 'white').

    Template: use {#if hero} for the two variants. Hero variant: `<div class="rounded-xl p-6 text-center w-full" style="background-color: {bgColor}; color: {textColor}"><div class="text-7xl font-bold leading-none">{grade}</div>{#if score !== undefined}<div class="text-sm text-gray-400 mt-1">{score} / 100</div>{/if}</div>`. Non-hero variant: `<span class="inline-flex items-center justify-center rounded px-2 py-0.5 text-xs font-bold w-12 h-6" style="background-color: {bgColor}; color: {textColor}">{grade}</span>`.

    Note: the score display in the hero variant uses `text-gray-400` regardless of tier — the tier color is only on the background, not the text.
  </action>
  <verify>
    <automated>cd "/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game" && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TierBadge.svelte accepts props: grade (TierGrade), score? (number), hero? (boolean, default false)
    - Hero variant renders a div with class containing 'text-7xl', 'font-bold', 'rounded-xl'
    - Non-hero variant renders a span with class containing 'text-xs', 'font-bold', 'w-12', 'h-6'
    - Both variants have inline style with `background-color: {bgColor}` where bgColor = tierToCssVar(grade)
    - When grade === 'God', textColor is '#1a1a1a' (dark); all other grades use 'white'
    - When hero=true and score is provided, shows "{score} / 100" below the grade letter
    - npx svelte-check exits with 0 TypeScript errors
  </acceptance_criteria>
  <done>
    TierBadge.svelte fully implemented with two variants. svelte-check exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: CharacterCard.svelte — full styled character sheet with New Character button</name>
  <files>src/components/CharacterCard.svelte</files>
  <read_first>
    - src/components/CharacterCard.svelte — current stub content (must read to know what to replace)
    - src/lib/game/scoreTier.ts — computeOverallScore and scoreTier function signatures
    - src/lib/session/types.ts — SpinResult interface (with optional tier/score fields)
    - .planning/phases/02-full-23-spin-game-loop/02-UI-SPEC.md — CharacterCard visual spec (card container, hero section, stats section, character details section, New Character button)
    - .planning/phases/02-full-23-spin-game-loop/02-PATTERNS.md — CharacterCard.svelte section (import pattern, $props, $derived values, template structure)
    - .planning/phases/02-full-23-spin-game-loop/02-CONTEXT.md — D-04 (functional+styled card), D-05 (hero badge shows grade + score), D-06 (New Character button, no confirmation)
  </read_first>
  <action>
    Replace the stub src/components/CharacterCard.svelte with the full implementation.

    Script section imports: import TierBadge from './TierBadge.svelte'; import { scoreTier, computeOverallScore } from '$lib/game/scoreTier'; import type { SpinResult } from '$lib/session/types'.

    Props: let { results, onNewCharacter }: { results: SpinResult[]; onNewCharacter: () => void } = $props().

    Constants: const STAT_CATEGORIES = ['strength','speed','agility','durability','iq','charisma','fightingSkill','potential','energyLevel','powerMastery','weaponMastery'] as const. Helper function formatCategoryName(cat: string): string — inserts space before uppercase letters: return cat.replace(/([A-Z])/g, ' $1').replace(/^./, c => c.toUpperCase()).trim().

    Derived values: const statResultsRecord = $derived(Object.fromEntries(STAT_CATEGORIES.map(cat => { const r = results.find(r => r.category === cat); return [cat, r?.score ?? 0] }))); const overallScore = $derived(computeOverallScore(statResultsRecord)); const overallTier = $derived(scoreTier(overallScore)). Per CLAUDE.md rule 4: scoreTier() is called only on the aggregated overallScore — tier for individual stats is read directly from result.tier (already embedded by the game loop).

    Separate results: const statResults = $derived(results.filter(r => r.tier !== undefined)); const nonStatResults = $derived(results.filter(r => r.tier === undefined));

    Template: outer `<div class="bg-gray-900 rounded-2xl p-8 max-w-2xl mx-auto w-full">`.

    Title: `<h2 class="text-3xl font-bold mb-6">Your Character</h2>`.

    Overall tier section: `<p class="text-sm text-gray-400 mb-2">Overall Tier</p>` then `<TierBadge grade={overallTier} score={overallScore} hero />`.

    Stats section (per UI-SPEC): `<h3 class="text-base font-bold mt-8 mb-3 text-gray-300">Stats</h3>` then each stat row using statResults array. Row structure: `<div class="flex items-center gap-3 py-2 border-b border-gray-800 last:border-0"><span class="text-sm text-gray-400 w-24 shrink-0">{formatCategoryName(result.category)}</span><TierBadge grade={result.tier} score={result.score} /><span class="text-xs text-gray-400 w-8 text-right">{result.score}</span><span class="text-sm text-white flex-1 truncate">{result.resultLabel}</span></div>`.

    Character Details section: `<h3 class="text-base font-bold mt-6 mb-3 text-gray-300">Character Details</h3>` then each non-stat row. Row: `<div class="flex items-start gap-3 py-2 border-b border-gray-800 last:border-0"><span class="text-sm text-gray-400 w-24 shrink-0">{formatCategoryName(result.category)}</span><span class="text-sm text-white flex-1">{result.resultLabel}</span></div>`.

    New Character button (D-06 — no confirmation, destructive red): `<button onclick={onNewCharacter} class="mt-8 w-full bg-red-600 hover:bg-red-700 text-white py-3 rounded-lg font-bold text-base transition-colors">New Character</button>`.
  </action>
  <verify>
    <automated>cd "/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game" && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5 && npx vitest run src/lib/</automated>
  </verify>
  <acceptance_criteria>
    - CharacterCard.svelte accepts props: results (SpinResult[]), onNewCharacter (() => void)
    - CharacterCard computes overallScore via computeOverallScore(statResultsRecord) — not by reading any stored tier value
    - CharacterCard computes overallTier via scoreTier(overallScore) — no manual grade lookup
    - Hero TierBadge is rendered with hero=true, grade=overallTier, score=overallScore
    - Stat rows (results with tier !== undefined): each shows TierBadge + numeric score + result label
    - Non-stat rows (results with tier === undefined): each shows category name + result label, no badge
    - "New Character" button calls onNewCharacter directly — no confirm dialog, no setTimeout
    - npx svelte-check exits with 0 TypeScript errors
    - npx vitest run src/lib/ exits 0 (all utility tests still pass)
  </acceptance_criteria>
  <done>
    CharacterCard.svelte fully implemented. Hero badge shows overall grade and score. Stat rows show TierBadge + score + label. Non-stat rows show label only. New Character button wired to onNewCharacter prop. svelte-check and vitest both exit 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    TierBadge and CharacterCard are fully implemented. The complete Phase 2 game loop now
    ends with a real styled character sheet instead of a placeholder. The overall tier badge
    displays the weighted-average computed grade at 72px. Each stat row has a colored badge,
    numeric score, and flavor label.
  </what-built>
  <how-to-verify>
    1. Run: `cd "/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game" && npm run dev`
    2. Open http://localhost:5173
    3. Play through ALL spins — Race, racial abilities, Archetype, archetype abilities, all fixed spins through to the end
    4. Verify CHARACTER CARD appears when all spins complete:
       - Title "Your Character" visible
       - Hero tier badge shows letter grade (e.g., "B+") at large size with numeric score (e.g., "52 / 100") below
       - Hero badge background color matches the tier (B+ = green, God = pale gold, F- = dark brown)
       - Stats section shows rows for Strength, Speed, Agility, Durability, IQ, Charisma, Fighting Skill, Potential, Energy Level, Power Mastery, Weapon Mastery — each with a colored TierBadge, numeric score, and flavor label
       - Character Details section shows non-stat results: Race, Archetype, Power, Weapon, Weaknesses (0–3), Title, etc.
       - "New Character" button is red, visible at the bottom
    5. Click "New Character" — verify:
       - No confirmation dialog or modal appears
       - Spin counter resets to "Spin 1 of 22"
       - Results panel shows "Your fate is being written..."
       - Wheel is ready to spin Race again
    6. Spin Race 3–5 times across separate sessions — verify different tier colors appear in the results panel TierBadge
    7. Verify God tier badge (if achievable by luck, or temporarily change one label's tier to 'God' to test): uses dark text (#1a1a1a) on pale gold background
    8. Verify ROADMAP Phase 2 success criteria mentally:
       - SC1: Correct order Race → abilities → Archetype → abilities → Backstory → ... → Title ✓
       - SC2: Stat wheels show flavor labels; landing shows tier grade + score ✓
       - SC3: Complete card with all stats, tier badges, overall tier badge ✓
       - SC4: New Character resets cleanly without refresh ✓
       - SC5: All 28 grades theoretically reachable (all tiers present in label data) ✓
  </how-to-verify>
  <resume-signal>Type "approved" when the character card is styled correctly and all success criteria pass, or describe specific visual/functional issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Plan 02-05 is pure display components consuming local state. No network, no user-generated HTML, no auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | Character card displaying game results | accept | Results are the product — intentionally shown. No sensitive data. |
| T-02-10 | Tampering | Overall tier computed from scoreTier() | accept | CLAUDE.md mandates scoreTier() as single source of truth. CharacterCard reads tier from SpinResult.tier (embedded at spin time) — never re-derives from label text. Overall grade uses computeOverallScore() + scoreTier() deterministically. |
</threat_model>

<verification>
Final Phase 2 automated check:

```bash
cd "/Users/bastianpethel/Documents/AI-AGENTS/wheel spin game" && npx vitest run && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5
```

Both must exit with 0 errors/failures. Then the human checkpoint above confirms the visual end-to-end.
</verification>

<success_criteria>
- TierBadge.svelte: hero and inline variants correct; tier-colored background; God uses dark text
- CharacterCard.svelte: overall hero badge, 11 stat rows, non-stat character details section, New Character button
- computeOverallScore() and scoreTier() used for overall tier — no manual grade computation
- "New Character" resets cleanly to Spin 1 of 22 without page reload (D-06)
- npx vitest run exits 0 (full test suite)
- npx svelte-check exits 0 (zero TypeScript errors)
- Human verifier approves the character card checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-full-23-spin-game-loop/02-05-SUMMARY.md`
</output>
