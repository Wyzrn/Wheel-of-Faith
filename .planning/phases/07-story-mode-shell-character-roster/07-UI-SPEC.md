---
phase: 7
slug: story-mode-shell-character-roster
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-18
---

# Phase 7 — UI Design Contract
## Story Mode Shell + Character Roster

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — manual Tailwind CSS 4 with custom CSS properties |
| Preset | not applicable |
| Component library | none (hand-crafted components using project utility classes) |
| Icon library | Material Symbols Outlined (already loaded in layout) |
| Font — body | Inter, sans-serif |
| Font — display/headings | Cinzel, serif (`--font-cinzel`) |
| Font — labels/mono | JetBrains Mono, monospace (`--font-mono`) |

Source: `src/app.css` `@theme` block — verified directly from codebase.

---

## Design Tokens (Existing — Do Not Redefine)

All tokens below are already declared in `src/app.css`. Phase 7 uses them as-is.

### Surface Tokens

| Token | Value | Usage |
|-------|-------|-------|
| `--color-background` / `--bg-void` | `#07070d` | Page background (body, `/story` root) |
| `--color-surface` | `#13121c` | Elevated surfaces: entry screen card, roster header |
| `--color-surface-container-low` | `#1b1b24` | Roster card background (resting state) |
| `--color-surface-container` | `#1f1f28` | Roster card hover / active state background |
| `--color-surface-container-high` | `#292933` | Modal overlay content panel background |
| `--color-surface-container-highest` | `#34343e` | Sort control button group background |

### Text Tokens

| Token | Value | Usage |
|-------|-------|-------|
| `--color-on-surface` | `#e4e1ee` | Primary text — character names, headings, body |
| `--color-on-surface-variant` | `#d2c5ae` | Secondary text — race label, archetype label, shard values in card |
| `--color-outline` | `#9a907b` | Muted text — sort labels, placeholder copy |
| `--color-outline-variant` | `#4e4635` | Inactive nav items, dimmed tier badge borders |

### Accent / Interactive Tokens

| Token | Value | Usage |
|-------|-------|-------|
| `--color-primary` / `--gold-bright` | `#ffdf96` | Story Mode title text, active sort button highlight, shard balance display |
| `--color-primary-dim` / `--gold-mid` / `--gold-glow` | `#f0c040` / `rgba(240,192,64,0.4)` | "Enter Story Mode" CTA button glow, "Generate New Character" hover glow |
| `--color-error` | `#ffb4ab` | Sell confirmation button border |
| `--tier-*` variables | (46-grade palette, see app.css) | TierBadge component — do not replicate these in Phase 7 |

---

## Spacing Scale

8-point scale. All layout values use multiples of 4px only.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gap inside roster card, badge inner padding |
| sm | 8px | Roster card internal padding, sort button padding |
| md | 16px | Roster card outer padding, modal body padding, entry screen content gap |
| lg | 24px | Roster grid gap (desktop), entry screen section spacing |
| xl | 32px | Entry screen card max-width padding, modal outer padding |
| 2xl | 48px | Entry screen vertical breathing room above/below CTA |
| 3xl | 64px | Page-level top padding (below the existing bottom-nav convention: body already has `padding-bottom: 72px`) |

Exception: touch targets on sell button and sort controls — minimum 44px height (mobile tap target standard). Apply via `min-h-11` (44px) in Tailwind.

---

## Typography

Declared sizes and weights. No additional sizes or weights may be introduced in Phase 7 components.

| Role | Size | Weight | Line Height | Font | Usage |
|------|------|--------|-------------|------|-------|
| Display | 28px (`text-[28px]`) | 700 (bold) | 1.2 | Cinzel (`font-cinzel`) | "Story Mode" entry screen title |
| Heading | 20px (`text-xl`) | 600 (semibold) | 1.25 | Cinzel (`font-cinzel`) | Roster section header ("Your Roster"), modal card title (character name in overlay header) |
| Body | 16px (`text-base`) | 400 (regular) | 1.5 | Inter (default) | Sell confirmation body copy, empty state body, shard balance number |
| Label | 14px (`text-sm`) | 600 (semibold) | 1.4 | JetBrains Mono (`font-mono`) | Roster card character name, race label, archetype label, sort control text, tier badge label (already handled by TierBadge), nav labels |

Source: Existing `nav-label` in layout.svelte uses `font-mono` 9px uppercase for nav — Phase 7 roster cards elevate to 14px for readability. Entry screen title follows CharacterCard title pattern.

---

## Color Contract

60 / 30 / 10 split:

| Role | Value | Split | Usage |
|------|-------|-------|-------|
| Dominant | `#07070d` (`--bg-void`) | 60% | Page background, overlay backdrop |
| Secondary | `#13121c` / `#1b1b24` (`--color-surface` / `--color-surface-container-low`) | 30% | Entry screen card surface, roster card resting background, modal content panel |
| Accent | `#f0c040` (`--gold-mid`) | 10% | See reserved list below |
| Destructive | `#ffb4ab` (`--color-error`) | — | Sell confirmation button border and text label only |

**Accent reserved for (explicit list):**
1. "Enter Story Mode" primary CTA button — `metal-stamp-gold` utility class (already in app.css)
2. "Generate New Character" primary CTA button — same `metal-stamp-gold` class
3. Shard balance display — `--gold-bright` (`#ffdf96`) text color for the numeric value
4. Active sort control button — `#f0c040` underline/highlight indicator
5. Story Mode entry screen title text — `gold-emboss` utility class (already in app.css)
6. Roster card tier badge border glow — tier-specific color from existing `--tier-*` variables (handled by TierBadge, not new accent usage)

Accent is NOT used on: inactive sort buttons, roster card borders (use `--color-outline-variant`), secondary CTAs, close/cancel buttons.

---

## Screen Inventory

### Screen 1: Story Mode Entry (`/story`, view = 'entry')

**Layout:** Full-page centered column. Body background `--bg-void`. Radial gold glow at top center (same `body` background from app.css is inherited — no new gradient needed).

**Elements (top to bottom):**
1. Story Mode title — "STORY MODE" — Display/28px/Cinzel/bold — `gold-emboss` utility class
2. Flavor subtitle — "Build your roster. Survive the Void." — Body/16px/Inter/400 — `--color-on-surface-variant`
3. Shard balance indicator — crystal/gem icon (Material Symbols: `diamond`) + numeric value — Label/14px/JetBrains Mono/semibold — value in `--gold-bright`, label in `--color-outline`
4. Roster count — "N / 50 characters" — Label/14px/JetBrains Mono — `--color-outline`
5. Primary CTA: "Enter Story Mode" button — `metal-stamp-gold` class — full-width up to 320px max
6. If roster has characters: secondary link "View Roster" — text-only, `--color-on-surface-variant`, underline on hover

**State: Roster at cap (50/50):** "Generate New Character" button is replaced by: "Roster Full — Sell a character to generate more" — muted text only, no button. Entry screen shows this state.

### Screen 2: Roster View (`view = 'roster'`)

**Layout:** Full-page. Sticky header bar (64px) containing: "ROSTER" heading (Heading/20px/Cinzel), shard balance (Label/14px/Mono), sort controls. Below: scrollable roster card grid.

**Sticky header:**
- Background: `rgba(7, 7, 13, 0.97)` with `backdrop-filter: blur(20px)` — matches existing `bottom-nav` pattern
- Border-bottom: `1px solid rgba(240, 192, 64, 0.15)` — matches existing nav border-top pattern
- Height: 64px

**Sort controls (inside sticky header, right-aligned):**
Three toggle buttons: "Tier" | "Race" | "Archetype"
- Resting: background `--color-surface-container-highest` (#34343e), text `--color-outline`
- Active: gold underline (2px `#f0c040` bottom border), text `--gold-bright`
- Font: Label/14px/JetBrains Mono/semibold
- Height: 44px min (touch target)
- Corner radius: 4px (`rounded`)

**Roster grid:**
- Mobile (< 640px): 2 columns, gap 8px
- Desktop (≥ 640px): 3 columns, gap 16px
- Max-width: 960px, centered

**Roster Card (RosterCard.svelte):**
- Background: `--color-surface-container-low` (#1b1b24)
- Border: `1px solid rgba(255,223,150,0.08)` resting; `1px solid rgba(240,192,64,0.25)` on hover
- Border-radius: 8px (`rounded-lg`)
- Padding: 12px (between sm and md — use `p-3`)
- Shadow: `0 2px 8px rgba(0,0,0,0.4)` resting; `0 4px 16px rgba(0,0,0,0.6)` hover
- Cursor: pointer (entire card is clickable for expansion)

  **Card content (top to bottom):**
  1. TierBadge (inline variant) — top-right corner, absolute positioned
  2. Character name — Label/14px/JetBrains Mono/semibold — `--color-on-surface` — single line, `truncate`
  3. Race — Label/14px/JetBrains Mono/400 — `--color-on-surface-variant` — single line, `truncate`
  4. Archetype — Label/14px/JetBrains Mono/400 — `--color-outline` — single line, `truncate`
  5. Sell button — bottom of card — "Sell" label + crystal icon — `metal-stamp-crimson` class — full card width — height 36px — stops click propagation (does not trigger card expand)

  **Card states:**
  - Resting: as above
  - Hover: border brightens, shadow deepens, card lifts `translateY(-2px)` — transition 120ms
  - Focused (keyboard): `outline: 2px solid #f0c040; outline-offset: 2px`

**"Generate New Character" button:**
- Position: below roster grid, full width up to 320px, centered
- Style: `metal-stamp-gold` class
- Text: "Generate New Character"
- Disabled state (at cap): muted background (#34343e), text (#9a907b), cursor not-allowed, no hover effect

### Screen 3: Spin Loop (`view = 'spin'`)

Reuses the existing `SpinWheel.svelte` component and main game spin loop visual patterns. No new design contract needed — this is a functional reuse. The spin loop renders within the `/story` page context. Background inherits `--bg-void` from body. No Story Mode branding overlay during spin (spin is immersive, full screen).

The only Story Mode–specific element: a passive header indicator (non-sticky, fades after 2s) showing "Story Mode" in Label/14px/Cinzel/`--color-outline` at top-left — confirms mode context without competing with the wheel.

### Screen 4: Character Sheet Overlay (`view = 'expanded'`)

**Overlay backdrop:** `rgba(7, 7, 13, 0.88)` — `backdrop-filter: blur(8px)` — full-screen, z-index 50. Click outside to dismiss.

**Overlay panel:**
- Background: `--color-surface-container-high` (#292933)
- Border: `1px solid rgba(255,223,150,0.12)`
- Border-radius: 12px (`rounded-xl`) on desktop; full-screen on mobile (bottom sheet style, `rounded-t-xl`)
- Max-width: 640px, centered on desktop; full-width with `rounded-t-xl` on mobile
- Max-height: 90vh, scrollable internally
- Animation: `slideInBottom` keyframe (already defined in app.css) — 200ms

**Panel content:**
- Header row: character name (Heading/20px/Cinzel) + close button (`×` — Body/20px, `--color-outline`, hover `--color-on-surface`) — 48px height
- Body: `CharacterCard` with `readonly={true}` — renders within the scroll area
- No footer (close via header button or backdrop click)

### Screen 5: Sell Confirmation Dialog (`sellTarget !== null`)

Rendered as a modal on top of whatever current view is active.

**Backdrop:** Same as overlay but `rgba(7, 7, 13, 0.72)` — lighter than character sheet so the roster remains partially visible behind.

**Dialog panel:**
- Background: `--color-surface-container-high` (#292933)
- Border: `1px solid rgba(255, 180, 171, 0.25)` — destructive tint (red-tinted border signals irreversible action)
- Border-radius: 12px (`rounded-xl`)
- Width: 320px fixed, centered horizontally and vertically
- Padding: 24px (`p-6`)
- Animation: `popIn` keyframe (already in app.css) — 180ms

**Dialog content:**
1. Icon — Material Symbols: `warning` — 24px — `#ffb4ab` (`--color-error`)
2. Heading — "Sell [Character Name]?" — Heading/20px/Cinzel/semibold — `--color-on-surface`
3. Tier note — "[Tier badge inline] [Tier grade]" — Body/16px/Inter — `--color-on-surface-variant`
4. Shard value — "You will receive [N] Fate Shards" — Body/16px/Inter — value in `--gold-bright`, surrounding text in `--color-on-surface-variant`
5. Warning line — "This action cannot be undone." — Label/14px/JetBrains Mono — `--color-error` (`#ffb4ab`)
6. Button row (Cancel | Confirm Sell):
   - Cancel: ghost style — background transparent, border `1px solid rgba(255,223,150,0.2)`, text `--color-on-surface-variant`, hover border `rgba(255,223,150,0.4)` — width 50% of row
   - Confirm Sell: `metal-stamp-crimson` class — width 50% of row

---

## Component Inventory

| Component | Status | Notes |
|-----------|--------|-------|
| `SpinWheel.svelte` | Reuse as-is | Story Mode spin loop |
| `CharacterCard.svelte` | Reuse with `readonly={true}` | Expanded roster card; pass `startedAt` from `StoryRosterEntry.sessionStartedAt` |
| `TierBadge.svelte` | Reuse as-is | Inline variant on roster cards; hero variant in sell dialog |
| `StoryEntryScreen.svelte` | New | Entry screen with title, shard balance, CTAs |
| `RosterCard.svelte` | New | Compact roster card; tier badge + name + race + archetype + sell button |
| `SellConfirmModal.svelte` | New | Confirmation dialog with shard value |

---

## Interaction Contract

### Roster Card Tap / Click
- **Trigger:** Click/tap anywhere on the roster card EXCEPT the Sell button
- **Result:** `expandedId = entry.id` → character sheet overlay appears
- **Animation:** `slideInBottom` (app.css keyframe), 200ms
- **Dismiss:** Click backdrop or close button → `expandedId = null`, overlay exits with `opacity 0` transition 150ms

### Sell Flow
1. User clicks Sell button on a roster card
2. Click is stopPropagated — card expand does NOT trigger
3. `sellTarget = entry` → `SellConfirmModal` renders with `popIn` animation
4. User reads: name, tier, shard value, irreversibility warning
5. Cancel → `sellTarget = null`, dialog closes
6. Confirm Sell → `handleSell()` runs atomically: removes entry from roster, credits shards, `sellTarget = null`
7. Roster re-renders reactively (Svelte 5 `$derived` sorted array updates)
8. Shard balance display updates immediately (same reactive cycle)

### Sort Controls
- Clicking an inactive sort button: updates `sortBy` state → `$derived sortedRoster` recalculates → grid re-renders
- No animation on sort reorder (instant swap — avoids GSAP conflict with spin sessions that may be loading)
- Active sort button: `#f0c040` 2px bottom-border, text color `--gold-bright`

### Roster Cap (50/50)
- "Generate New Character" button: `disabled` attribute set, `cursor-not-allowed`, opacity 0.4
- Caption appears below button: "Sell a character to free a roster slot" — Label/14px/JetBrains Mono — `--color-outline`

### Keyboard Navigation
- All interactive elements (roster cards, sell button, sort controls, dialog buttons) must receive focus via Tab
- Focused state: `outline: 2px solid #f0c040; outline-offset: 2px`
- Escape key closes overlay or dialog (whichever is open; dialog takes priority)
- Tab trap inside dialog (Cancel ↔ Confirm Sell loop)

### View Transitions
- Entry → Roster: no SvelteKit view-transition (same-route state change, not a navigation)
- Entry → Spin: same page, state machine transition — fade opacity 0 → 1 over 200ms using CSS transition on the view container
- Spin complete → Roster: no transition animation needed; roster appears immediately with the new card prepended

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Story Mode entry title | "STORY MODE" |
| Entry subtitle | "Build your roster. Survive the Void." |
| Primary CTA — entry | "Enter Story Mode" |
| Primary CTA — new character | "Generate New Character" |
| Roster header | "Your Roster" |
| Shard balance label | "Fate Shards" |
| Roster count label | "[N] / 50 characters" |
| Empty roster heading | "Your Roster is Empty" |
| Empty roster body | "Generate a new character to begin your story." |
| Roster cap CTA replacement | "Roster Full — Sell a character to generate more" |
| Roster cap button caption | "Sell a character to free a roster slot" |
| Sort controls | "Tier" / "Race" / "Archetype" |
| Sell button label (on roster card) | "Sell" |
| Sell dialog heading | "Sell [Character Name]?" |
| Sell dialog tier note | "[tier badge] [tier grade]" |
| Sell dialog shard line | "You will receive [N] Fate Shards" |
| Sell dialog warning | "This action cannot be undone." |
| Sell dialog cancel | "Cancel" |
| Sell dialog confirm | "Confirm Sell" |
| Sell success (implicit — no toast) | Roster updates immediately; shard balance increments — no toast copy needed in Phase 7 |
| Error — localStorage unavailable | "Story Mode requires local storage. Enable it in your browser settings to continue." — shown in place of entry CTA |

**Tone rule:** All Story Mode copy uses the same epic/absurd blend as the main game. Functional labels (Cancel, Sell, Tier) are concise. Flavor copy (subtitle, empty state) leans dramatic. No exclamation points on destructive actions.

---

## Story Mode Visual Identity

Phase 7 operates within the existing dark design system. The isolation from the main game is expressed through **absence of shared navigation elements**, not through a completely different color scheme.

Specific isolation signals:
- No bottom navigation bar on `/story` — Story Mode uses its own in-page navigation (entry → spin → roster are view states, not routes). The existing bottom nav from `+layout.svelte` WILL render (it's in the root layout). Story Mode does NOT add a "Back to Main" link anywhere.
- Entry screen uses the `obsidian-slab` utility class for its central card — same material language as the main game's result cards, but Story Mode context is established purely by the "STORY MODE" heading and the absence of main-game session state.
- No Wheel of Fate logo or main game branding on the Story Mode entry screen.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No third-party component registry is used. All new components are hand-crafted Svelte components using existing Tailwind CSS 4 utility classes and the project's established `app.css` utility classes (`metal-stamp-gold`, `metal-stamp-crimson`, `obsidian-slab`, `gold-emboss`, `engraved-text`, `carved-groove`).

---

## Pre-Population Sources

| Source | Decisions Used |
|--------|---------------|
| `src/app.css` | All color tokens, spacing-via-utility, font declarations, utility classes (`metal-stamp-gold`, `metal-stamp-crimson`, `obsidian-slab`, `gold-emboss`, `pulseGold`, animation keyframes) |
| `src/routes/+layout.svelte` | Bottom nav pattern (backdrop blur, border, height 64px), Material Symbols Outlined icon set, JetBrains Mono nav label pattern, nav-tab active state gold treatment |
| `src/components/TierBadge.svelte` | Inline vs hero variant API; tier color via CSS var; font-bold + rounded treatment |
| `src/components/CharacterCard.svelte` | Required `startedAt` prop, `readonly` prop, `onNewCharacter` callback requirement |
| `07-RESEARCH.md` | View state machine (`entry | spin | roster | expanded`), `SellConfirmModal` atomic sell pattern, `RosterCard` component names, localStorage isolation rules, CharacterCard pitfall (startedAt required) |
| `REQUIREMENTS.md` | ROST-01 (auto-names, never editable), ROST-02 (50-slot cap, sortable by tier/race/archetype), ROST-03 (full sheet on click), ROST-04 (sell → shard value → confirmation → irreversible) |
| `ROADMAP.md` Phase 7 | Isolation rule: zero visual connection to main game beyond shared tokens; no back-to-main-game navigation bridge |
| User input | None required — all design contract questions answered by upstream artifacts |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
