---
phase: 07
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/routes/story/+page.svelte
  - src/components/story/RosterCard.svelte
  - src/components/story/SellConfirmModal.svelte
autonomous: true
requirements:
  - ROST-02
  - ROST-03
  - ROST-04
tags:
  - svelte
  - sveltekit
  - story-mode
  - ui

must_haves:
  truths:
    - "Navigating to /story renders the Story Mode entry screen without reading any wof_* localStorage key"
    - "Entry screen shows the 'STORY MODE' title, shard balance, roster count (N / 50), and an 'Enter Story Mode' gold CTA"
    - "When roster is non-empty, the entry screen exposes navigation to the roster view"
    - "Roster view renders one RosterCard per entry in a responsive grid (2 cols mobile, 3 cols desktop)"
    - "Roster is sortable by 'tier' | 'race' | 'archetype' via three sort buttons in a sticky header"
    - "Active sort button uses metal-stamp-gold styling; inactive uses neutral background"
    - "Sort state is held in $state and the sorted array is computed via $derived — no manual sync"
    - "Clicking a roster card (anywhere except the Sell button) sets expandedId and renders CharacterCard readonly inside an obsidian-slab overlay"
    - "Clicking 'Sell Character' on a card sets sellTarget and renders SellConfirmModal; click propagation is stopped so the card does not expand"
    - "SellConfirmModal displays the entry's name, TierBadge, and getShardValue(entry.overallTier) before the user confirms"
    - "Confirming sell atomically: removes the entry from the roster, increments shards by getShardValue, and closes the modal — all in one state-change cycle"
    - "Roster array is persisted to localStorage on every mutation via $effect calling saveRoster(roster)"
    - "Shards value is persisted to localStorage on every mutation via $effect calling saveShards(shards)"
    - "Roster and shards survive a full page reload (loaded in onMount via loadRoster / loadShards)"
  artifacts:
    - path: "src/routes/story/+page.svelte"
      provides: "Story Mode root route — view state machine (entry | roster | expanded | sell)"
      contains: "view: 'entry' | 'spin' | 'roster' | 'expanded'"
    - path: "src/components/story/RosterCard.svelte"
      provides: "Compact roster card with TierBadge + name + race + archetype + sell button"
      exports: ["default"]
    - path: "src/components/story/SellConfirmModal.svelte"
      provides: "Confirmation dialog showing shard value with Keep / Confirm Sell buttons"
      exports: ["default"]
  key_links:
    - from: "src/routes/story/+page.svelte"
      to: "src/lib/story/store.ts"
      via: "loadRoster, saveRoster, loadShards, saveShards"
      pattern: "from '\\$lib/story/store'"
    - from: "src/routes/story/+page.svelte"
      to: "src/lib/story/shards.ts"
      via: "getShardValue (atomic sell handler)"
      pattern: "from '\\$lib/story/shards'"
    - from: "src/routes/story/+page.svelte"
      to: "src/components/CharacterCard.svelte"
      via: "readonly expanded sheet overlay"
      pattern: "<CharacterCard"
    - from: "src/components/story/RosterCard.svelte"
      to: "src/components/TierBadge.svelte"
      via: "inline tier badge"
      pattern: "<TierBadge"
    - from: "src/components/story/SellConfirmModal.svelte"
      to: "src/lib/story/shards.ts"
      via: "getShardValue($derived)"
      pattern: "from '\\$lib/story/shards'"
---

<objective>
Build the Story Mode shell: a new SvelteKit route at `/story` with an isolated view state machine (entry / roster / expanded / sell), plus two new components — `RosterCard` (compact card with sell button) and `SellConfirmModal` (atomic sell confirmation). Wire the roster sort, expand-to-CharacterCard overlay, and the atomic sell flow.

Purpose: This plan delivers ROST-02 (roster grid + sortable + persisted), ROST-03 (expand to full sheet), and the UI half of ROST-04 (sell confirmation flow). The spin loop that produces new roster entries lands in Plan 03; this plan can be tested today by seeding `localStorage.story_roster` manually in DevTools.

Output:
- `src/routes/story/+page.svelte` — entry + roster + expand + sell views (spin view stubbed to "Coming in Plan 03")
- `src/components/story/RosterCard.svelte` — compact card with TierBadge + name + race + archetype + sell
- `src/components/story/SellConfirmModal.svelte` — confirm dialog
- ROST-02, ROST-03, and ROST-04 (UI portion) demonstrably working with manually seeded roster data

Out of scope (deferred to Plan 03):
- The actual Story Mode spin loop view (`view === 'spin'`) — stub a "Coming soon" panel that returns the user to `view = 'entry'` and DOES NOT touch `wof_*` keys.
- Backend writes (`story_characters` collection) — Phase 8+ per 07-RESEARCH.md Assumption A2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-story-mode-shell-character-roster/07-RESEARCH.md
@.planning/phases/07-story-mode-shell-character-roster/07-PATTERNS.md
@.planning/phases/07-story-mode-shell-character-roster/07-UI-SPEC.md
@.planning/phases/07-story-mode-shell-character-roster/07-01-SUMMARY.md

# Source-of-truth components and styles
@src/lib/story/types.ts
@src/lib/story/store.ts
@src/lib/story/shards.ts
@src/components/CharacterCard.svelte
@src/components/TierBadge.svelte
@src/app.css
@src/routes/+page.svelte
@src/routes/+layout.svelte
@src/routes/+layout.js

<interfaces>
<!-- Extracted contracts so the executor does not need to grep. -->

From src/components/CharacterCard.svelte props:
- `results: SpinResult[]` (required)
- `name?: string` (default '')
- `startedAt: string` (REQUIRED — pass `entry.sessionStartedAt`)
- `readonly?: boolean` (default false)
- `rivalsWins?: number` (default 0)
- `onNewCharacter: () => void` (REQUIRED — pass `() => {}` no-op in readonly story overlay)
- `onBackToMenu?: () => void`

From src/components/TierBadge.svelte props:
- `grade: TierGrade` (required)
- `score?: number`
- `hero?: boolean` (default false — inline variant for roster cards)
- `displayLabel?: string`

From src/lib/story/store.ts (Plan 01):
- `loadRoster(): StoryRosterEntry[]`
- `saveRoster(roster: StoryRosterEntry[]): void`
- `loadShards(): number`
- `saveShards(amount: number): void`
- `addToRoster(roster, entry): StoryRosterEntry[] | null` (cap-enforced; null at 50)
- `MAX_ROSTER_SIZE: number` (= 50)

From src/lib/story/shards.ts (Plan 01):
- `getShardValue(tier: TierGrade): number`

From src/lib/story/types.ts (Plan 01):
- `StoryRosterEntry { id, name, race, archetype, overallScore, overallTier, spins, createdAt, sessionStartedAt }`
- `StoryState { roster, shards }`

CSS utility classes (from src/app.css — VERIFIED):
- `obsidian-slab` — entry card / modal panel material
- `metal-stamp-gold` — primary CTA (Enter Story Mode, Generate New Character active state, active sort button)
- `metal-stamp-crimson` — destructive (Sell Character button, Confirm Sell)
- `gold-emboss` — entry title text effect
- `noise-overlay` — texture inside obsidian-slab (requires `relative overflow-hidden` parent)
- Keyframes: `slideInBottom` (200ms) for expanded overlay; `popIn` (180ms) for sell modal

SvelteKit routing rule (VERIFIED via src/routes/+layout.js):
- Root layout exports `ssr = false`; `/story` inherits this — DO NOT create `src/routes/story/+layout.js`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RosterCard.svelte + SellConfirmModal.svelte components</name>
  <files>
    src/components/story/RosterCard.svelte,
    src/components/story/SellConfirmModal.svelte
  </files>
  <read_first>
    - src/lib/story/types.ts
    - src/lib/story/shards.ts
    - src/components/TierBadge.svelte (Svelte 5 runes + $props pattern)
    - src/routes/+page.svelte (lines 1855–1888 — resume prompt modal as analog for SellConfirmModal)
    - src/app.css (utility classes: obsidian-slab, metal-stamp-gold, metal-stamp-crimson, noise-overlay; keyframes: slideInBottom, popIn)
    - .planning/phases/07-story-mode-shell-character-roster/07-UI-SPEC.md (Screen 2 RosterCard styling; Screen 5 SellConfirmModal styling; Copywriting Contract)
    - .planning/phases/07-story-mode-shell-character-roster/07-PATTERNS.md (RosterCard and SellConfirmModal sections)
  </read_first>
  <behavior>
    **RosterCard.svelte:**
    - Props: `{ entry: StoryRosterEntry, onExpand: (id: string) =&gt; void, onSell: (entry: StoryRosterEntry) =&gt; void }` via `$props()`
    - Renders a clickable card. Clicking anywhere on the card except the Sell button calls `onExpand(entry.id)`.
    - Card contains: `<TierBadge grade={entry.overallTier} />` absolute top-right; entry.name (Label/14px/JetBrains Mono/bold); entry.race (Label/14px/Mono/regular, `--color-on-surface-variant`); entry.archetype (Label/14px/Mono/regular, `--color-outline`); a "Sell Character" button at the bottom with class `metal-stamp-crimson`, full card width, `min-h-[36px]`.
    - Sell button onclick handler MUST call `event.stopPropagation()` before invoking `onSell(entry)` so it does not trigger card expansion.
    - Card styling: `background: var(--color-surface-container-low)`, `border: 1px solid rgba(255,223,150,0.08)` resting / `rgba(240,192,64,0.25)` on hover, `rounded-lg`, `p-2` (8px) padding, `cursor-pointer`, hover lifts `translateY(-2px)` over 120ms transition.
    - Keyboard accessible: tabindex=0; Enter or Space triggers expand; focus outline `2px solid #f0c040` with `outline-offset: 2px`.
    - Name/race/archetype each `truncate` to single line.

    **SellConfirmModal.svelte:**
    - Props: `{ entry: StoryRosterEntry, onConfirm: () =&gt; void, onCancel: () =&gt; void }` via `$props()`
    - `let value = $derived(getShardValue(entry.overallTier))`
    - Backdrop: `fixed inset-0 z-50 flex items-center justify-center px-4` with `background: rgba(7,7,13,0.72)` and `backdrop-filter: blur(8px)`. Clicking the backdrop calls `onCancel`. Clicking inside the dialog stops propagation.
    - Dialog panel: `obsidian-slab w-80 rounded-xl p-6` with `border: 1px solid rgba(255,180,171,0.25)`, animation `popIn 180ms ease-out` on mount.
    - Dialog content top-to-bottom (per 07-UI-SPEC.md Screen 5):
      1. Material Symbols `warning` icon, 24px, `--color-error`
      2. Heading: `Sell {entry.name}?` — Cinzel 20px bold, `--color-on-surface`
      3. Inline TierBadge + tier grade — Inter 16px, `--color-on-surface-variant`
      4. `You will receive {value} Fate Shards` — value styled in `--gold-bright`, rest in `--color-on-surface-variant`
      5. `This action cannot be undone.` — JetBrains Mono 14px, `--color-error`
      6. Two buttons in a `flex gap-3` row (each 50% width):
         - "Keep Character" — ghost style: `background: transparent; border: 1px solid rgba(255,223,150,0.2); color: var(--color-on-surface-variant)`; onclick = `onCancel`
         - "Confirm Sell" — `metal-stamp-crimson`; onclick = `onConfirm`
    - Escape key calls `onCancel` (svelte:window keydown handler).
    - Focus trap: Tab cycles between Keep Character and Confirm Sell only.
    - Both components use Svelte 5 runes (`$props`, `$derived`, `$state` as needed) — NO Svelte stores.
  </behavior>
  <action>
    Create the directory `src/components/story/` and author both components.

    `RosterCard.svelte`: Use `$props()` destructuring with the type annotation shown in the interfaces block. Use TierBadge inline (no `hero` prop). Render character name with class `text-sm font-mono font-bold truncate`, race with `text-sm font-mono truncate` and inline style color `var(--color-on-surface-variant)`, archetype same but color `var(--color-outline)`. Sell button label is exactly the string `Sell Character`. The sell button onclick MUST be `(e) => { e.stopPropagation(); onSell(entry); }`. The card root onclick is `() => onExpand(entry.id)`. Add `onkeydown` to handle Enter/Space → `onExpand(entry.id)`.

    `SellConfirmModal.svelte`: Use `$props()` with `{ entry, onConfirm, onCancel }`. Use `$derived` for `value`. The Confirm Sell label is exactly the string `Confirm Sell`. The Keep Character label is exactly the string `Keep Character`. The warning line copy is exactly `This action cannot be undone.`. Use `<svelte:window onkeydown={...}>` to call `onCancel()` when `event.key === 'Escape'`. Animation: add `style="animation: popIn 180ms ease-out;"` to the dialog panel inline.

    Do NOT add a default `name="Sell..."` to either prop block — `entry` is required and untyped null/undefined inputs are not handled in Phase 7.
  </action>
  <verify>
    <automated>npx svelte-check --threshold error 2>&amp;1 | tee /tmp/p07-02-task1.log; ! grep -q "error" /tmp/p07-02-task1.log</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/components/story/RosterCard.svelte` exists and contains `import TierBadge from`
    - Source: RosterCard contains `entry: StoryRosterEntry`, `onExpand:`, `onSell:` in its $props destructuring
    - Source: RosterCard contains the string literal `Sell Character`
    - Source: RosterCard sell button onclick contains `stopPropagation`
    - Source: `src/components/story/SellConfirmModal.svelte` exists and contains `getShardValue` import
    - Source: SellConfirmModal contains the string literal `Confirm Sell`
    - Source: SellConfirmModal contains the string literal `Keep Character`
    - Source: SellConfirmModal contains the string literal `This action cannot be undone.`
    - Source: SellConfirmModal contains `class="metal-stamp-crimson"` on the confirm button
    - Source: SellConfirmModal contains `getShardValue(entry.overallTier)`
    - Behavior: `npx svelte-check --threshold error` exits 0
    - Behavior: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both components compile with svelte-check; props use Svelte 5 runes; literal copy strings match the Copywriting Contract in 07-UI-SPEC.md.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: /story route page — entry, roster, expanded, sell view state machine</name>
  <files>src/routes/story/+page.svelte</files>
  <read_first>
    - src/routes/+page.svelte (entire file, focusing on lines 86–108 state declarations, 578–602 onMount, 1462–1471 $state.snapshot pattern, 1855–1888 modal overlay)
    - src/routes/+layout.js (VERIFY ssr=false; the story route MUST NOT add its own +layout.js)
    - src/routes/+layout.svelte (bottom-nav coexistence; story page MUST NOT add its own bottom nav)
    - src/components/story/RosterCard.svelte (created in Task 1)
    - src/components/story/SellConfirmModal.svelte (created in Task 1)
    - src/components/CharacterCard.svelte (props signature for readonly overlay)
    - src/components/TierBadge.svelte
    - src/lib/story/store.ts
    - src/lib/story/shards.ts
    - src/lib/story/types.ts
    - src/app.css (utility classes referenced in UI-SPEC)
    - .planning/phases/07-story-mode-shell-character-roster/07-UI-SPEC.md (all five screens — entry, roster, spin stub, expanded overlay, sell dialog)
    - .planning/phases/07-story-mode-shell-character-roster/07-PATTERNS.md (Story-route section)
    - .planning/phases/07-story-mode-shell-character-roster/07-RESEARCH.md (Pitfall 1 isolation, Pitfall 5 SSR, Pattern 5 runes, Pattern 7 atomic sell)
  </read_first>
  <behavior>
    - File path is exactly `src/routes/story/+page.svelte`. No `+layout.js` is created in `src/routes/story/`.
    - The file imports `{ onMount }` from svelte and the story store/shards/types modules. It MUST NOT import `$lib/session/store`, `$lib/spinHistory`, or reference any `wof_*` localStorage key.
    - View state machine: `let view = $state<'entry' | 'spin' | 'roster' | 'expanded'>('entry')`.
    - Roster + shards reactive state: `let roster = $state<StoryRosterEntry[]>([])`, `let shards = $state(0)`.
    - Sort + dialog state: `let sortBy = $state<'tier' | 'race' | 'archetype'>('tier')`, `let expandedId = $state<string | null>(null)`, `let sellTarget = $state<StoryRosterEntry | null>(null)`.
    - `let sortedRoster = $derived([...roster].sort(...))` with comparator: tier → `b.overallScore - a.overallScore`; race → `a.race.localeCompare(b.race)`; archetype → `a.archetype.localeCompare(b.archetype)`.
    - `onMount(() => { roster = loadRoster(); shards = loadShards(); })`.
    - Two `$effect` blocks: `$effect(() => { saveRoster($state.snapshot(roster)) })` and `$effect(() => { saveShards(shards) })`. Use `$state.snapshot()` before `JSON.stringify`/`saveRoster` to avoid Svelte 5 Proxy serialization issues.
    - **Entry view (view === 'entry'):** Render full-page centered column per 07-UI-SPEC.md Screen 1. Title "STORY MODE" with `gold-emboss` class (Cinzel 28px bold). Subtitle "Build your roster. Survive the Void." (Inter 16px, `--color-on-surface-variant`). Shard balance row: Material Symbols `diamond` icon + `{shards}` in `--gold-bright` + label "Fate Shards" (JetBrains Mono 14px). Roster count line: `{roster.length} / 50 characters` (Mono 14px, `--color-outline`). Primary CTA: button labeled "Enter Story Mode" with class `metal-stamp-gold`, sets `view = 'spin'` on click (spin view itself is a stub in Plan 02). If `roster.length > 0`, also render a secondary text link "View Roster" (no button styling, underline on hover) that sets `view = 'roster'`. When `roster.length >= MAX_ROSTER_SIZE`, the primary CTA is replaced with the muted text "Roster Full — Sell a character to generate more" — no clickable button.
    - **Spin view (view === 'spin') — STUB:** Show a centered `obsidian-slab` panel with heading "Story Mode Spin" and body "Coming in Plan 03." plus a single button "Back" that sets `view = 'entry'`. MUST NOT instantiate any spin wheel logic in this plan. This is a deliberate stub.
    - **Roster view (view === 'roster'):** Sticky header bar (64px) per Screen 2: heading "Your Roster" (Cinzel 20px bold), shard balance display (Mono 14px), three sort toggle buttons "Tier" / "Race" / "Archetype". Active sort button uses `metal-stamp-gold` (or per UI-SPEC: gold underline + `--gold-bright` text); inactive uses neutral surface background `--color-surface-container-highest` and `--color-outline` text. Each sort button is `min-h-11` (44px touch target). Below header: roster grid `grid grid-cols-2 sm:grid-cols-3 gap-2 sm:gap-4 max-w-[960px] mx-auto p-4` rendering `<RosterCard entry={...} onExpand={...} onSell={...} />` per `sortedRoster`. Empty roster state: heading "Your Roster is Empty" + body "Generate a new character to begin your story." + a "Generate New Character" CTA (`metal-stamp-gold`, sets `view = 'spin'`). Below the grid (when non-empty and under cap), the same "Generate New Character" CTA appears centered, max-width 320px. When at cap (50), the CTA is `disabled` with `cursor-not-allowed opacity-40` and a caption below: "Sell a character to free a roster slot" (Mono 14px, `--color-outline`).
    - Handler `function handleExpand(id: string) { expandedId = id; view = 'expanded'; }`. Handler `function handleSell(entry: StoryRosterEntry) { sellTarget = entry; }` (does NOT mutate roster yet — only opens dialog).
    - **Expanded view (view === 'expanded' && expandedId !== null):** Renders the roster underneath plus a fixed `inset-0 z-50` overlay with `background: rgba(7,7,13,0.88)` and `backdrop-filter: blur(8px)`. Clicking the backdrop sets `expandedId = null; view = 'roster';`. The inner panel uses class `obsidian-slab w-full max-w-[640px] max-h-[90vh] overflow-y-auto rounded-xl` with animation `slideInBottom 200ms ease-out`. Panel header: 48px row with the character's name (Cinzel 20px bold) and a close button (×, `aria-label="Close character sheet"`) that mirrors the backdrop click handler. Panel body: `<CharacterCard results={entry.spins} name={entry.name} startedAt={entry.sessionStartedAt} readonly={true} onNewCharacter={() => {}} />`. Inner panel stops click propagation so clicks inside do not close.
    - **Sell dialog (sellTarget !== null):** Renders `<SellConfirmModal entry={sellTarget} onConfirm={confirmSell} onCancel={() => sellTarget = null} />` regardless of which view is active. Atomic confirm handler:
      `function confirmSell() { if (!sellTarget) return; const value = getShardValue(sellTarget.overallTier); roster = roster.filter(r => r.id !== sellTarget!.id); shards += value; sellTarget = null; }` — all four state changes in one synchronous function so the reactive cycle is atomic.
    - The page MUST NOT call `loadSession`, `saveSession`, `clearSession`, `loadSpinHistory`, or any function from `$lib/session/store` or `$lib/spinHistory`.
    - All Tailwind utility classes used must exist in Tailwind 4 (the project already uses 4.3.0 — standard classes apply).
  </behavior>
  <action>
    Create the `src/routes/story/` directory and author `+page.svelte`. Follow 07-UI-SPEC.md verbatim for all five screens (entry, spin stub, roster, expanded overlay, sell dialog). Use the exact copy strings listed in the Copywriting Contract. Wire all four state variables (`view`, `roster`, `shards`, `sortBy`, `expandedId`, `sellTarget`) with `$state`, derive `sortedRoster` with `$derived`, persist with `$effect`.

    Imports MUST include:
    - `import { onMount } from 'svelte'`
    - `import { loadRoster, saveRoster, loadShards, saveShards, MAX_ROSTER_SIZE } from '$lib/story/store'`
    - `import { getShardValue } from '$lib/story/shards'`
    - `import type { StoryRosterEntry } from '$lib/story/types'`
    - `import CharacterCard from '../../components/CharacterCard.svelte'` (or `$lib`-aliased equivalent matching existing convention in `src/routes/+page.svelte`)
    - `import TierBadge from '../../components/TierBadge.svelte'`
    - `import RosterCard from '../../components/story/RosterCard.svelte'`
    - `import SellConfirmModal from '../../components/story/SellConfirmModal.svelte'`

    Imports MUST NOT include any of: `$lib/session/store`, `$lib/spinHistory`, `loadSession`, `saveSession`, `clearSession`, `loadSpinHistory`. Verify post-write with a grep gate.

    Do NOT create `src/routes/story/+layout.js` (the root layout's `ssr = false` is inherited).

    The Story Mode spin stub in this plan is a placeholder panel — Plan 03 replaces it with the real spin loop. Make the stub visibly identify itself with the heading "Story Mode Spin" and body copy "Coming in Plan 03." so checkers can confirm it is not the live spin loop.
  </action>
  <verify>
    <automated>npx svelte-check --threshold error 2>&amp;1 | tee /tmp/p07-02-task2.log; ! grep -q "error" /tmp/p07-02-task2.log</automated>
  </verify>
  <acceptance_criteria>
    - Source: file exists at `src/routes/story/+page.svelte`
    - Source: file does NOT exist at `src/routes/story/+layout.js` (`test ! -f src/routes/story/+layout.js`)
    - Source: file contains `view = $state&lt;'entry' | 'spin' | 'roster' | 'expanded'`
    - Source: file contains `let roster = $state` and `let shards = $state`
    - Source: file contains `let sortBy = $state` with the three sort options
    - Source: file contains `let sortedRoster = $derived(`
    - Source: file contains `onMount(` and calls both `loadRoster()` and `loadShards()`
    - Source: file contains `$effect(() =&gt; { saveRoster(` and `$effect(() =&gt; { saveShards(`
    - Source: file uses `$state.snapshot(` at least once when persisting roster
    - Source: file contains the literal string `STORY MODE`
    - Source: file contains the literal string `Enter Story Mode`
    - Source: file contains the literal string `Your Roster`
    - Source: file contains the literal string `Your Roster is Empty`
    - Source: file contains the literal string `Generate New Character`
    - Source: file contains the literal string `Roster Full — Sell a character to generate more`
    - Source: file contains a CharacterCard mount with `readonly={true}` and `startedAt={` referencing sessionStartedAt
    - Source: file contains the literal string `Coming in Plan 03.` (spin-view stub marker)
    - Isolation: `grep -E "(\\\$lib/session/store|\\\$lib/spinHistory|loadSession|saveSession|clearSession|wof_session|wof_spin_history)" src/routes/story/+page.svelte` returns nothing
    - Behavior: `npx svelte-check --threshold error` exits 0
    - Behavior: `npx tsc --noEmit` exits 0
    - Behavior: Manually setting `localStorage.setItem('story_roster', JSON.stringify([{id:'t1',name:'Test Hero',race:'Human',archetype:'Knight',overallScore:50,overallTier:'C',spins:[],createdAt:'2026-05-18T00:00:00Z',sessionStartedAt:'2026-05-18T00:00:00Z'}]))` and reloading `/story` renders an entry-screen roster count of `1 / 50 characters` (this is a manual checkpoint for /gsd-verify-work)
  </acceptance_criteria>
  <done>/story route renders entry, roster, expanded, and sell views; roster and shards persist via $effect+localStorage; sell flow atomically removes character and credits shards; no main-game state is read; spin view is a clearly marked stub.</done>
</task>

</tasks>

<verification>
- `npx svelte-check --threshold error` exits 0
- `npx tsc --noEmit` exits 0
- `grep -rE "(\\\$lib/session/store|\\\$lib/spinHistory|wof_session|wof_spin_history)" src/routes/story/ src/components/story/` returns nothing
- `test ! -f src/routes/story/+layout.js` (root SSR=false must be inherited)
- All copy strings from 07-UI-SPEC.md Copywriting Contract present in source (Enter Story Mode, Your Roster, Sell Character, Confirm Sell, Keep Character, This action cannot be undone., etc.)
- Manual: seeding `story_roster` via DevTools and visiting `/story` shows the entry screen → "View Roster" → roster grid with the seeded card → click expands to CharacterCard readonly → "Sell Character" opens SellConfirmModal → "Confirm Sell" removes card and credits shards.
</verification>

<success_criteria>
1. `/story` renders an entry screen showing the gold "Enter Story Mode" CTA, "Fate Shards" balance, and "{N} / 50 characters" count (ROST-02 surface)
2. Seeding a roster entry in localStorage and reloading shows that entry in the roster grid (ROST-02 persistence)
3. Roster grid is sortable by Tier / Race / Archetype with the active sort visually highlighted (ROST-02 sortable)
4. Clicking a roster card opens an overlay containing `<CharacterCard readonly={true}>` with all spin data rendered (ROST-03)
5. Clicking "Sell Character" opens `<SellConfirmModal>` showing the entry's shard value from `getShardValue()`; "Confirm Sell" atomically removes the entry and credits shards; "Keep Character" / Escape / backdrop click closes without mutation (ROST-04 UI)
6. No file in `src/routes/story/` or `src/components/story/` imports from `$lib/session/store`, `$lib/spinHistory`, or references any `wof_*` localStorage key (isolation rule from ROADMAP.md)
</success_criteria>

<output>
After completion, create `.planning/phases/07-story-mode-shell-character-roster/07-02-SUMMARY.md` summarizing:
- New files (paths, line counts)
- View state machine transitions (entry → spin stub → roster → expanded → sell)
- Confirmed isolation: grep result showing no main-game store imports
- Manual test trace: seeded roster entry → expand → sell → verify shard balance
- Any deferred items (the spin view stub — replaced by Plan 03)
</output>
