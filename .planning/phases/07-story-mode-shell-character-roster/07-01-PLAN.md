---
phase: 07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/story/types.ts
  - src/lib/story/naming.ts
  - src/lib/story/shards.ts
  - src/lib/story/store.ts
  - src/lib/story/naming.test.ts
  - src/lib/story/shards.test.ts
  - src/lib/story/store.test.ts
autonomous: true
requirements:
  - ROST-01
  - ROST-02
  - ROST-04
tags:
  - svelte
  - localStorage
  - prng
  - story-mode

must_haves:
  truths:
    - "generateCharacterName() returns the same string for the same (overallScore, raceLabel) inputs across calls"
    - "generateCharacterName() returns an 'Adjective Noun' string with exactly one ASCII space and no Math.random() involvement"
    - "loadRoster() returns [] when localStorage is empty, missing, or contains corrupted JSON"
    - "saveRoster() + loadRoster() round-trips a StoryRosterEntry[] without data loss"
    - "addToRoster() returns null when roster length is already 50 and never writes to localStorage in that case"
    - "addToRoster() prepends the new entry (most recent first) when under cap and writes the updated array to localStorage"
    - "loadShards() returns 0 when story_shards key is absent or malformed"
    - "getShardValue('F-') === 20, getShardValue('D+') === 55, getShardValue('SS') === 500, getShardValue('Absolute+') === 2000"
    - "getShardValue() returns a finite positive number for all 46 TierGrade union members (no fallback 20 hit on a valid grade above F-)"
  artifacts:
    - path: "src/lib/story/types.ts"
      provides: "StoryRosterEntry, StoryState interfaces"
      contains: "export interface StoryRosterEntry"
    - path: "src/lib/story/naming.ts"
      provides: "generateCharacterName(overallScore, raceLabel) deterministic seeded PRNG"
      exports: ["generateCharacterName"]
    - path: "src/lib/story/store.ts"
      provides: "loadRoster, saveRoster, loadShards, saveShards, addToRoster, MAX_ROSTER_SIZE"
      exports: ["loadRoster", "saveRoster", "loadShards", "saveShards", "addToRoster", "MAX_ROSTER_SIZE"]
    - path: "src/lib/story/shards.ts"
      provides: "getShardValue(tier) covering all 46 TierGrade values"
      exports: ["getShardValue"]
    - path: "src/lib/story/naming.test.ts"
      provides: "ROST-01 determinism + format coverage"
    - path: "src/lib/story/store.test.ts"
      provides: "ROST-02 localStorage CRUD + 50-cap enforcement"
    - path: "src/lib/story/shards.test.ts"
      provides: "ROST-04 all-bracket coverage (F- → Absolute+)"
  key_links:
    - from: "src/lib/story/types.ts"
      to: "src/lib/game/scoreTier.ts"
      via: "import type { TierGrade }"
      pattern: "import type.*TierGrade.*scoreTier"
    - from: "src/lib/story/types.ts"
      to: "src/lib/session/types.ts"
      via: "import type { SpinResult }"
      pattern: "import type.*SpinResult.*session/types"
    - from: "src/lib/story/shards.ts"
      to: "src/lib/game/scoreTier.ts"
      via: "import type { TierGrade }"
      pattern: "import type.*TierGrade"
    - from: "src/lib/story/store.ts"
      to: "src/lib/story/types.ts"
      via: "import type { StoryRosterEntry }"
      pattern: "import type.*StoryRosterEntry.*types"
---

<objective>
Establish the Story Mode data foundation: type definitions, deterministic auto-naming, localStorage CRUD with 50-slot cap enforcement, and the Fate Shard sell-value lookup. All pure TypeScript modules under `src/lib/story/` with full Vitest coverage. No UI in this plan.

Purpose: Plans 02 and 03 depend on a stable, tested data layer. Writing it first with tests means the UI work in subsequent plans never has to question whether `addToRoster` enforces the cap or whether `getShardValue` handles all 46 tier grades — the contracts are locked.

Output:
- `src/lib/story/types.ts` — StoryRosterEntry and StoryState interfaces
- `src/lib/story/naming.ts` — `generateCharacterName()` using mulberry32 seeded PRNG
- `src/lib/story/store.ts` — `loadRoster`, `saveRoster`, `loadShards`, `saveShards`, `addToRoster` with cap enforcement
- `src/lib/story/shards.ts` — `getShardValue()` for all 46 TierGrade values
- Three Vitest test files covering ROST-01, ROST-02, ROST-04
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-story-mode-shell-character-roster/07-RESEARCH.md
@.planning/phases/07-story-mode-shell-character-roster/07-PATTERNS.md
@.planning/phases/07-story-mode-shell-character-roster/07-VALIDATION.md

# Source-of-truth files the executor MUST consult for existing patterns
@src/lib/session/types.ts
@src/lib/session/store.ts
@src/lib/session/store.test.ts
@src/lib/game/scoreTier.ts
@src/lib/game/random.ts
@src/lib/spinHistory.ts

<interfaces>
<!-- Extracted from src/lib/session/types.ts and src/lib/game/scoreTier.ts so the executor does not need to grep. -->

From src/lib/session/types.ts:
- `export interface SpinResult { step: number; category: string; resultLabel: string; resultIndex: number; timestamp: string; tier?: TierGrade; score?: number; displayLabel?: string }`

From src/lib/game/scoreTier.ts:
- `export type TierGrade = 'F-' | 'F' | 'F+' | 'E-' | 'E' | 'E+' | 'D-' | 'D' | 'D+' | 'C-' | 'C' | 'C+' | 'B-' | 'B' | 'B+' | 'A-' | 'A' | 'A+' | 'S-' | 'S' | 'S+' | 'SS-' | 'SS' | 'SS+' | 'SSS-' | 'SSS' | 'SSS+' | 'Z-' | 'Z' | 'Z+' | 'ZZ-' | 'ZZ' | 'ZZ+' | 'ZZZ-' | 'ZZZ' | 'ZZZ+' | 'Celestial-' | 'Celestial' | 'Celestial+' | 'Godly-' | 'Godly' | 'Primordial' | 'Primordial+' | 'Absolute-' | 'Absolute' | 'Absolute+' | 'God'` (46 grades total — VERIFY exact union by reading the file before authoring shards.ts)
- `export function computeOverallScore(results: SpinResult[]): number`
- `export function scoreTier(score: number): TierGrade`

From src/lib/session/store.ts (pattern to mirror, NOT to import in story code):
- `function createSession(): SessionState`
- `function loadSession(): SessionState | null`
- `function saveSession(session: SessionState): void`
- `function clearSession(): void`
- localStorage guard pattern: `if (typeof localStorage === 'undefined') return null` on every read/write
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Story types module — StoryRosterEntry and StoryState interfaces</name>
  <files>src/lib/story/types.ts</files>
  <read_first>
    - src/lib/session/types.ts (interface-only file pattern; `import type` usage)
    - src/lib/game/scoreTier.ts (TierGrade union to import)
    - .planning/phases/07-story-mode-shell-character-roster/07-PATTERNS.md (Story-specific interface section)
  </read_first>
  <behavior>
    - Module is interface-only: no runtime code, no functions, no default export
    - Two named exports: `StoryRosterEntry`, `StoryState`
    - `StoryRosterEntry` includes `sessionStartedAt: string` (required, used as `startedAt` prop on CharacterCard) — NOT `startedAt`
    - All imports use `import type` syntax to avoid circular runtime deps
    - tsc passes with no errors when this file is imported by store.ts
  </behavior>
  <action>
    Create `src/lib/story/types.ts` as an interface-only module. Import types only:
    - `import type { SpinResult } from '$lib/session/types'`
    - `import type { TierGrade } from '$lib/game/scoreTier'`

    Export `StoryRosterEntry` with fields:
    - `id: string` — populated by `crypto.randomUUID()` at write time
    - `name: string` — auto-generated, permanent (set once via `generateCharacterName`)
    - `race: string` — denormalized from spins for sort/filter
    - `archetype: string` — denormalized from spins for sort/filter
    - `overallScore: number` — from `computeOverallScore()` at session end
    - `overallTier: TierGrade` — from `scoreTier(overallScore)` at session end; stored, never re-derived at display time
    - `spins: SpinResult[]` — full result array for CharacterCard rendering
    - `createdAt: string` — ISO timestamp of when entry was added to roster
    - `sessionStartedAt: string` — ISO timestamp captured when the story spin session began; passed as `startedAt` prop to CharacterCard

    Export `StoryState` with fields:
    - `roster: StoryRosterEntry[]`
    - `shards: number`

    No default export. No runtime code. Comment each interface with one-line purpose.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2>&amp;1 | grep -v "^\s*$" | tee /tmp/p07-01-tsc.log; test ! -s /tmp/p07-01-tsc.log || (echo "tsc errors after types.ts" &amp;&amp; false)</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/lib/story/types.ts` contains `export interface StoryRosterEntry {`
    - Source: `src/lib/story/types.ts` contains `sessionStartedAt: string`
    - Source: `src/lib/story/types.ts` contains `overallTier: TierGrade`
    - Source: `src/lib/story/types.ts` contains `export interface StoryState {`
    - Source: file uses `import type` (not bare `import`) for both `SpinResult` and `TierGrade`
    - Behavior: `grep -c "^export default" src/lib/story/types.ts` returns 0
    - Behavior: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>types.ts exists, exports StoryRosterEntry (with sessionStartedAt) and StoryState, no default export, tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Naming + Shards + Store modules with full Vitest coverage</name>
  <files>
    src/lib/story/naming.ts,
    src/lib/story/shards.ts,
    src/lib/story/store.ts,
    src/lib/story/naming.test.ts,
    src/lib/story/shards.test.ts,
    src/lib/story/store.test.ts
  </files>
  <read_first>
    - src/lib/story/types.ts (created in Task 1)
    - src/lib/session/store.ts (localStorage guard pattern + CRUD shape)
    - src/lib/session/store.test.ts (localStorage mock in beforeEach — copy verbatim)
    - src/lib/game/random.ts (pure-function module file shape for naming.ts)
    - src/lib/game/scoreTier.ts (full TierGrade union; iterate every grade in shards.test.ts)
    - src/lib/game/scoreTier.test.ts (test layout pattern for pure functions)
    - src/lib/spinHistory.ts (cap-enforcement pattern reference)
    - .planning/phases/07-story-mode-shell-character-roster/07-PATTERNS.md (sections for store.ts, shards.ts, naming.ts, and tests — implementations to follow)
    - .planning/phases/07-story-mode-shell-character-roster/07-RESEARCH.md (Pattern 2 mulberry32 algorithm; Pattern 4 shard brackets)
  </read_first>
  <behavior>
    `naming.ts`:
    - Pure module, no imports from other project files, no default export
    - Internal `mulberry32(seed: number): () => number` and `hashStr(s: string): number` helpers (not exported)
    - `ADJECTIVES` and `NOUNS` const arrays each containing at least 40 entries (target: ≥ 1,600 unique combinations)
    - `generateCharacterName(overallScore: number, raceLabel: string): string` returns `${adj} ${noun}`
    - Seed formula: `(overallScore * 31 + hashStr(raceLabel)) | 0`
    - No `Math.random()` calls anywhere in the file
    - Deterministic: same inputs always produce the same output

    `shards.ts`:
    - Pure module, only `import type { TierGrade } from '$lib/game/scoreTier'`
    - Exports `getShardValue(tier: TierGrade): number`
    - F–D bracket (`F-`,`F`,`F+`,`E-`,`E`,`E+`,`D-`,`D`,`D+`): linearly interpolated 20→55 by tier index
    - C–B bracket (`C-`,`C`,`C+`,`B-`,`B`,`B+`): linearly interpolated 70→130
    - A–S bracket (`A-`,`A`,`A+`,`S-`,`S`,`S+`): linearly interpolated 160→320
    - `SS-` and `SS`: return 500
    - SS+ and every tier above (SS+, SSS-, SSS, SSS+, Z*, ZZ*, ZZZ*, Celestial*, Godly*, Primordial, Primordial+, Absolute-, Absolute, Absolute+, God): `Math.min(2000, 500 + (indexInAboveArray + 1) * 100)`
    - `Absolute+` and `God` always return 2000 (capped)
    - Function exhaustively covers all 46 TierGrade values without hitting the fallback path

    `store.ts`:
    - Imports `import type { StoryRosterEntry } from './types'` only
    - Constants: `ROSTER_KEY = 'story_roster'`, `SHARDS_KEY = 'story_shards'`, `MAX_ROSTER_SIZE = 50` (exported)
    - `loadRoster(): StoryRosterEntry[]` — guards `typeof localStorage === 'undefined'`, returns `[]` on miss/corrupt
    - `saveRoster(roster: StoryRosterEntry[]): void` — guards localStorage; `JSON.stringify`
    - `loadShards(): number` — guards localStorage, `parseInt(... ?? '0', 10)`, returns 0 on miss/NaN
    - `saveShards(amount: number): void` — guards localStorage, writes `String(amount)`
    - `addToRoster(roster: StoryRosterEntry[], entry: StoryRosterEntry): StoryRosterEntry[] | null` — returns `null` when `roster.length >= MAX_ROSTER_SIZE` (no write performed); otherwise returns `[entry, ...roster]` and calls `saveRoster(updated)`
    - Module MUST NOT import from `$lib/session/store`, `$lib/spinHistory`, or reference any `wof_*` localStorage key

    Tests:
    - `naming.test.ts`: determinism (same inputs → identical output across 2 calls), variability (a sample of 20 different `(score, race)` pairs produces ≥ 15 distinct names), format (`/^[A-Z][a-z]+ [A-Z][a-z]+$/` matches output), no `Math.random` spy hit (mock `Math.random` to throw and confirm name still generates)
    - `shards.test.ts`: copy `ALL_TIER_GRADES` array (46 entries) and assert each returns a finite number ≥ 20; explicit asserts for `F-` === 20, `D+` === 55, `C-` === 70, `B+` === 130, `A-` === 160, `S+` === 320, `SS-` === 500, `SS` === 500, `SS+` === 600, `Absolute+` === 2000, `God` === 2000
    - `store.test.ts`: copy the localStorage mock from `session/store.test.ts` verbatim in `beforeEach`; test `loadRoster` returns `[]` on empty/corrupt; `saveRoster`+`loadRoster` round-trips; `loadShards` returns 0 on missing; `saveShards`+`loadShards` round-trips; `addToRoster` prepends + writes when under cap; `addToRoster` returns `null` and does NOT write when length is 50; assert no `wof_*` keys are read or written (inspect mock store keys after operations)
  </behavior>
  <action>
    Create six files in `src/lib/story/`. For each, follow the patterns enumerated in 07-PATTERNS.md (sections for store.ts, shards.ts, naming.ts, and tests). Concrete contracts:

    1. `naming.ts` — implement mulberry32 seeded PRNG and `generateCharacterName(overallScore, raceLabel)`. Seed expression: `(overallScore * 31 + hashStr(raceLabel)) | 0`. ADJECTIVES and NOUNS const arrays of 40+ entries each (start from RESEARCH.md Pattern 2 seed lists; expand to ≥ 40 each). No `Math.random()` calls.

    2. `shards.ts` — implement `getShardValue(tier: TierGrade): number` per the bracket logic in 07-PATTERNS.md. The `TIERS_ABOVE_SS` array is the index source for the `500 + (idx + 1) * 100` formula. Cap at 2000 via `Math.min(2000, ...)`. Verify the full 46-grade `TierGrade` union by reading `src/lib/game/scoreTier.ts` first; if the union differs from PATTERNS.md, follow the file in the codebase.

    3. `store.ts` — implement `loadRoster`, `saveRoster`, `loadShards`, `saveShards`, `addToRoster`, and export `MAX_ROSTER_SIZE = 50`. Use only the two localStorage keys `story_roster` and `story_shards`. NEVER import `$lib/session/store` or read any `wof_*` key.

    4. `naming.test.ts` — Vitest. No localStorage mock needed. Cover determinism, variability sampling, format regex, and a `Math.random` poison check (`vi.spyOn(Math, 'random').mockImplementation(() => { throw new Error('Math.random forbidden') })`).

    5. `shards.test.ts` — Vitest. Define `ALL_TIER_GRADES: TierGrade[]` containing all 46 grades (extract by reading `src/lib/game/scoreTier.ts`). Iterate and assert `getShardValue(g)` returns finite number ≥ 20 with no NaN. Add explicit boundary asserts listed in the behavior block.

    6. `store.test.ts` — Vitest. Copy the localStorage mock pattern from `src/lib/session/store.test.ts` verbatim in `beforeEach`. Cover round-trips, default returns, cap enforcement at exactly length 50, and absence of any `wof_*` keys in the mock store after running roster/shards operations.

    Do not embed fenced code blocks here — refer to 07-PATTERNS.md and 07-RESEARCH.md sections for full reference implementations.
  </action>
  <verify>
    <automated>npx vitest run src/lib/story/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/lib/story/naming.ts` contains `function mulberry32(`
    - Source: `src/lib/story/naming.ts` contains `export function generateCharacterName(`
    - Source: `grep -v '^\s*\/\/' src/lib/story/naming.ts | grep -c 'Math.random'` returns 0 (no Math.random in non-comment code)
    - Source: `src/lib/story/naming.ts` ADJECTIVES array has ≥ 40 entries (`grep -c "'" src/lib/story/naming.ts` is a weak proxy; primary check is the test file's variability assertion)
    - Source: `src/lib/story/shards.ts` contains `export function getShardValue(tier: TierGrade): number`
    - Source: `src/lib/story/store.ts` contains `const ROSTER_KEY = 'story_roster'`
    - Source: `src/lib/story/store.ts` contains `const SHARDS_KEY = 'story_shards'`
    - Source: `src/lib/story/store.ts` contains `export const MAX_ROSTER_SIZE = 50`
    - Source: `src/lib/story/store.ts` contains `export function addToRoster(`
    - Source: `grep -E "(wof_session|wof_spin_history|loadSession|saveSession|clearSession)" src/lib/story/store.ts` returns nothing
    - Source: `grep -E "(\\$lib/session/store|\\$lib/spinHistory)" src/lib/story/store.ts` returns nothing
    - Behavior: `npx vitest run src/lib/story/ --reporter=verbose` exits 0
    - Behavior: vitest output shows ≥ 12 passing tests across naming.test.ts, shards.test.ts, store.test.ts (Wave 0 coverage from 07-VALIDATION.md)
    - Behavior: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All six files exist; vitest run on src/lib/story/ is green; tsc clean; no main-game localStorage keys referenced.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/story/` exits 0 with all Wave 0 tests passing
- `npx tsc --noEmit` exits 0
- No references to `wof_session`, `wof_spin_history`, `loadSession`, `saveSession`, or `$lib/session/store` in `src/lib/story/` (`grep -rE "(wof_session|wof_spin_history|\\$lib/session/store)" src/lib/story/` returns nothing)
- `src/lib/story/types.ts` has no `export default`
- Files modified by this plan are exclusively under `src/lib/story/`
</verification>

<success_criteria>
1. `StoryRosterEntry` interface exists with `sessionStartedAt: string` (matches CharacterCard pitfall in 07-RESEARCH.md Pitfall 3)
2. `generateCharacterName(overallScore, raceLabel)` is deterministic — calling twice with same inputs yields identical output
3. `addToRoster()` returns `null` and does not write to localStorage when roster length is 50
4. `getShardValue()` returns a finite number for every TierGrade union member (F- through Absolute+ / God)
5. `loadRoster()` returns `[]` on missing/corrupt data; `loadShards()` returns 0 on missing data
6. No file in `src/lib/story/` imports from `$lib/session/store` or references `wof_*` localStorage keys
</success_criteria>

<output>
After completion, create `.planning/phases/07-story-mode-shell-character-roster/07-01-SUMMARY.md` summarizing:
- Files created (paths + line counts)
- Test counts per file
- ADJECTIVES.length × NOUNS.length combination count
- Confirmation that no main-game localStorage keys are referenced
- Any deviations from 07-PATTERNS.md (with rationale)
</output>
